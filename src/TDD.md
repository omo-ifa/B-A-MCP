# TDD.md — Technical Design Decisions

> **TBD policy.** When you encounter a TBD reference in any document, look it up in the Master TBD Tracker below. If status is **open**: stub it, leave a `TODO: TBD-XXX` comment, and continue. **Do not guess** at the resolution. This file lives in `src/`, beside the code it binds to.

Stack and implementation conventions for this server — TypeScript ESM, `NodeNext`, `node:test`, the `index.ts`/`server.ts` split, the structured error envelope, and the context-budget ledger — live in [`src/CONTEXT.md`](CONTEXT.md). This file holds one thing: the canonical TBD tracker.

---

## Master TBD Tracker

This is the canonical TBD log. **All TBDs from all other documents resolve here.**

When a TBD is resolved:
1. Update **Status** to `Resolved` and fill in **Resolution**.
2. Record the reasoning in `planning/decisions/YYYY-MM-DD_title.md` (this tracker holds the status; the decision record holds the why).
3. Remove it from `CLAUDE.md`'s Key Open TBDs table.
4. Update any document that referenced it.
5. Build the feature that was blocked.

| TBD ID | Description | Blocks | Status | Resolution |
|---|---|---|---|---|
| TBD-1 | Does claude-mem's upstream repo ship an Apache 2.0 `NOTICE` file? | `THIRD_PARTY_NOTICES.md` — Apache 2.0 compliance | **Resolved** | **YES** — a `NOTICE` (242 B, `Copyright 2026 Alex Newman`) exists at repo root (`e2d1df5`); Apache-2.0 §4(d) requires reproducing its contents in the claude-mem notice block. See `planning/decisions/2026-08-20_tbd-1-claude-mem-notice.md`. |
| TBD-2 | Confirm each of the **five** bundled components' license from its own `LICENSE`/`plugin.json` (now includes `icm-architect`), not third-party listings | All packaging; `THIRD_PARTY_NOTICES.md`, `Integration_Spec.md` | Open (**rescoped 2026-08-20**) | Four of five confirmed from their own files (survey: `planning/calibration/2026-08-20_provenance-and-routing-census.md` §1): superpowers MIT, claude-mem Apache-2.0, task-observer CC-BY-4.0 (holder from README, not the bare CC `LICENSE.txt`), icm-architect MIT. **caveman is the open sub-item:** dual **MIT + BSL-1.1** — the bundled unit `skills/caveman/` is MIT (2 files, no engine embed, zero path overlap with BSL dirs; audit in census §5), but the notice must **path-scope to `skills/caveman/`**, must **not** imply whole-repo MIT, and must **pin a commit** (`package.json` 2.2.0 ≠ tag `pi-v0.1.0`). Widening the bundle beyond `skills/caveman/` requires a **fresh path audit**, not a blanket `skills/*` pin. |
| TBD-3 | Verify DO Functions free allowance against the live console (25,000 vs 90,000 GiB-s) | Cost model only — not load-bearing | Open | |
| TBD-4 | Do B&A docs reproduce ICM (Van Clief & McDermott, 2026) expression, or paraphrase it? **Escalated:** `icm-architect` is a 905-star MIT expression of the same paper, now bundled — resolve before the notices file ships and before any copy claims the methodology as B&A-original | Publication | Open (**narrowed 2026-08-20**) | Provenance separated from license: the `icm-architect` **code copyright is Jake Van Clief alone** (`LICENSE`, MIT, 2026); McDermott is a **paper co-author**, not a code-license holder (census §1). So the **MIT notice** (Van Clief, path to the bundled files) and the **reproduce-vs-paraphrase question** are cleanly separable — the notice can proceed without settling paraphrase. The paraphrase call remains the **product owner's** and still blocks the notices file / any "B&A-original methodology" copy. |
| TBD-5 | Paid-tier price and structure (one-time vs. subscription) | `export_record` checkout | Open | |
| TBD-6 | Package/repo name | Everything — nothing scaffolds unnamed | **Resolved** | Repo `B-A-MCP`; npm package `b-a-mcp`. Recorded in `planning/decisions/`. |
| TBD-7 | Pinned Superpowers major version | Dependency stability | **Resolved** | Pin **`v6.3.0`** commit **`b36e082`** (MIT, `Copyright (c) 2025 Jesse Vincent`, upstream `github.com/obra/superpowers`). Pin the commit, not the tag. See `planning/decisions/2026-08-20_tbd-7-superpowers-pin.md`. |
| TBD-8 | Split launch (free tier first) or single launch? | Sequencing | Open | |
| TBD-9 | `doc_drift` scope — which frameworks and migration formats are in scope for v1 | `doc_drift` build | Open | |
| TBD-10 | `context_audit` sub-score → headline **weighting** function (accuracy cluster > bloat; N/A sub-score drops and reweights). | `context_audit` composite `score` | Open | **Principle resolved** (accuracy > bloat). **Structural prerequisite RESOLVED 2026-08-20** — every sub-score now carries `n` and an empty denominator reports `null` ("not assessed"), never a misleading 100 that would weight like a verified check; see `planning/decisions/2026-08-20_subscore-confidence-signal.md`. **Weight NUMBERS remain open, data-blocked.** Current stub `TBD_10_WEIGHTS` (`src/tools/context-audit/score.ts`): `broken_refs:3, routing_drift:3, orphans:2, coverage:2, bloat:1`. **Do NOT resolve from B-A-MCP's run alone** (backtick routing → degenerate `refsFromRoots`/`refsFromNonRoots` denominators); needs ≥2–3 hyperlink-routed calibration repos. **Run-2 (2026-08-20, `planning/calibration/2026-08-20_context-audit-run-2-post-parser-fix.md`): still DEFERRED.** Now weighting ranges over **four** sub-scores, not five — `broken_refs` is removed (`planning/decisions/2026-08-20_broken-refs-removed-four-subscores.md`). `routing_drift` was non-discriminating (100 everywhere) until the router-path-drift rule (`planning/decisions/2026-08-20_router-path-drift.md`) gave it teeth; re-assess its weight after run-3. **Run-5 (2026-08-20, app sample) produced NO valid drift data** — all four app repos route via a root `AGENTS.md` the tool did not recognize (invisible routers). The fix has **landed** (`planning/designs/2026-08-20_agents-md-router-recognition-design.md`); numbers now gated on the nine-repo re-run only. **Run-6 (2026-08-24, the nine-repo re-run, `planning/calibration/2026-08-24_context-audit-run-6-nine-repo-rerun.md`): the `AGENTS.md` gate is CLEARED but TBD-10 is RE-BLOCKED for a new reason.** `routing_drift` now fires at volume (posthog n431, caveman n385) — but a filesystem check of all 59 drift findings found precision ≈ **17 %** (26/59 prose-relative false positives, 10 placeholder tokens, ~10 plausibly genuine). It carries the joint-highest stub weight (`3`); weighting a sub-score that measures prose is the same error class as the rejected `6000`. **Now blocked on TBD-16.** **`/decisions` 2026-08-24 — WEIGHT NUMBERS REMAIN DEFERRED, and the eligible sub-score set has narrowed.** Two of the four sub-scores are now **excluded from the composite pending correctness fixes**, neither exclusion being a weighting decision: **(a)** `routing_drift` contributes a **correctness-driven `null`** until its precision fix lands and is re-validated (`planning/decisions/2026-08-24_routing-drift-precision-and-interim-disposition.md` D2) — a weight of `0` was explicitly **rejected** because `ROUTING_LAYER_KEYS` is separate from `TBD_10_WEIGHTS`, so a zero-weighted drift still satisfies the §5 headline guard while contributing nothing, leaving hygiene-only `bloat` to carry the headline (measured: icm-architect 23 → **90**); **(b)** `orphans` stays out of the weighting until directory-granularity reachability lands (`planning/decisions/2026-08-24_orphans-routes-to-dirs-not-docs.md` D3). With `bloat`'s aggregation also shape-blocked, **`coverage` is currently the only routing-layer sub-score eligible to carry the headline** — a narrowness to watch, not a reason to relax either guard (§5 holds: a `coverage` measurement, floor of 0 included, is a routing-layer measurement). **`TBD_10_WEIGHTS` / `ROUTING_LAYER_KEYS` in `src/tools/context-audit/score.ts` are NOT edited by any of this.** |
| TBD-11 | `context_audit` **bloat thresholds** — routing-token-weight, inline-ratio, chain-depth cutoffs → severity. | `context_audit` `bloat` sub-score | Open | Data-blocked — needs more calibration repos (run-1 against B-A-MCP alone is insufficient). Stub constants live in **`src/tools/context-audit/bloat.ts`** — **read them from source, do not trust a value quoted here** (corrected 2026-08-24: this row previously named `TBD_11_ROUTING_TOKEN_CUTOFF=4000` and `TBD_11_DEPTH_CUTOFF=4`, neither of which exists any more; the per-router reshape renamed them). Values as of 2026-08-24: `TBD_11_ROUTER_TOKEN_CUTOFF=3000`, `TBD_11_CHAIN_TOKEN_CUTOFF=6000`, `TBD_11_CHAIN_DEPTH_CUTOFF=4`, `TBD_11_INLINE_RATIO_CUTOFF=0.85`, `TBD_11_INLINE_MIN_TOKENS=200`. **`TBD_11_CHAIN_TOKEN_CUTOFF=6000` is NOT the rejected `6000`** — the rejected number was a flat-total routing-token cutoff; this one is a root→leaf chain sum, a different metric. The rejection below stands. **`ROUTING_TOKEN_CUTOFF=6000` was CONSIDERED and REJECTED (2026-08-20):** the calibration-run-1 doc proposed raising 4000→6000, but that 6000 was chosen to stop the tool flagging **its own repo's** routing layer (B-A-MCP measures 4619 routing tokens) — tautological self-tuning (the repo is not a neutral baseline), and a single repo cannot constrain the cutoff's top end, which needs a repo with a genuinely bloated (10k+ token) router. **Stub stays 4000** until such a repo is calibrated. Inline-ratio/min-tokens/depth: no contrast data on B-A-MCP → unchanged. **SHAPE RESOLVED 2026-08-20** — see `planning/decisions/2026-08-20_tbd-11-bloat-per-router-not-total.md`: bloat is **per-router + per root→leaf chain**, NOT a flat total (run-2's caveman — 45,419 tokens / 23 routers ≈ <2,000 each — showed a flat total fails a healthy large monorepo). Per-router size needs no new data (walk already yields it); the **chain sum needs the router→router adjacency exposed from `buildGraph` into `scoreBloat`** (walk does not produce edges; the current `depth` proxy is filesystem-path depth, not chain depth). Cutoff NUMBERS remain open, data-blocked; implementation deferred. **Run-5 (2026-08-20):** posthog's ≈9 897-token root `AGENTS.md` + 44-file `AGENTS.md` tree is a real top-end candidate. The fix has **landed** (`planning/designs/2026-08-20_agents-md-router-recognition-design.md`); posthog's root `AGENTS.md` + tree is now measurable — capture it in the nine-repo re-run. **Run-6 (2026-08-24): top-end data UNBLOCKED, shape RE-BLOCKED.** Real datapoints at last — posthog 45 routers / 155 280 routing tokens / 9 834-token root `AGENTS.md`; cal.com's 15 125-token `agents/skills/vercel-react-best-practices/AGENTS.md`; first-ever `chain_depth` fire (posthog, 5). But two **shape** defects must be fixed before any cutoff is set: **(A)** the penalty is a flat sum over **all** routers with no normalization, so router *count* drives the score to the floor — caveman (40 routers, mean **1 651** tokens/router) scores **0/n40** while superpowers (1 router, **2 211** tokens) scores **90/n1**, and superset (mean 5 082) scores 45. The 2026-08-20 per-router decision moved the *metric* per-router but left the *aggregation* a flat sum — run-2's original complaint, one level up. **(B)** `INLINE_RATIO_CUTOFF=0.85` is a constant, not a discriminator: it produces **86 of ~117** bloat findings and fires on **8/9** repos, including a **262-token** `claude-mem/CLAUDE.md` (ratio 0.86) and superpowers' reference-quality router (0.98); `INLINE_MIN_TOKENS=200` is far too low. `router_tokens`/`chain_tokens`/`chain_depth` DO discriminate (26/100 routers over the size cutoff) and are the metrics worth weighting. **No cutoff set.** Route the shape fix through `/decisions` first. **`/decisions` 2026-08-24 — RESOLVED (shape finding + authorisation); CUTOFF NUMBERS REMAIN DEFERRED.** See `planning/decisions/2026-08-24_tbd-11-bloat-aggregation-shape.md`: the 2026-08-20 per-router ruling stands and is **not** re-litigated, but the implementation only half-satisfies it — the *metric* moved per-router while the *penalty aggregation* stayed a flat sum over every router, so router **count** drives the floor. A **shape re-work is authorised as its own build loop** (how per-router and per-chain penalties combine into one bounded sub-score without count dominating — normalisation / worst-case / per-chain-only all candidates, **none chosen**). `INLINE_RATIO_CUTOFF` **flagged for re-derivation** in that same loop (whether the metric survives at all is part of it). The data blocker is cleared; a **shape** blocker replaces it. **No number set or re-derived.** |
| TBD-12 | `context_audit` **coverage significance + thresholds** — source-vs-config classification, min file count for "significant", ancestor-coverage-within-N-hops vs own-router. Data-blocked — calibrate from the first dogfood run. **Build guard:** both the `coverage` (source, high) and `coverage_test` (test dir, medium) "uncovered significant workspace" findings must be gated behind a `TODO: TBD-12` so neither fires on uncalibrated defaults. | `context_audit` `coverage` sub-score | Open | Test-dir-vs-source **severity policy** sub-question RESOLVED — see `planning/decisions/2026-08-20_test-dir-coverage-severity.md` (test dirs stay significant but emit `coverage_test`/medium instead of `coverage`/high). The significance **threshold numbers** remain open, data-blocked (need more repos with varied source layouts; run-1 gives one significant/non-significant boundary pair, too thin). Current stubs (`src/tools/context-audit/coverage.ts`): `TBD_12_MIN_FILES=5`, `TBD_12_SOURCE_EXTS=[.ts, .js, .tsx, .jsx, .py, .go, .rs, .java, .rb]`. **Run-2 (2026-08-20): real layout spread now (significant-dir counts 0 / 1 / 2 / 35 / 50 at MIN_FILES=5); floor-to-0 policy validated (claude-mem coverage 0/n50). Still DEFERRED** — claude-mem's 50 significant dirs from a small repo raises "is MIN_FILES=5 too low?"; the data poses the question, does not answer it. Related invariant ratified: headline is a definite number whenever ≥1 significant dir, null allowed at 0 (`planning/decisions/2026-08-20_headline-definite-when-significant-dirs.md`). **Run-4 thin-coverage datapoint:** superpowers scored `coverage 100/n1` resting on a **single** significant directory — `tests/brainstorm-server`, the only dir with ≥5 source files (a **test** dir). Not a bug, but a coverage of 100 anchored on one test dir is thin and bears directly on the `MIN_FILES` question (is 5 too low/high, and should the significance basis weight source vs test dirs). **Run-5 (2026-08-20):** `coverage` **routing-basis guard RESOLVED** — `coverage` reports `null` when `routing_files > 0 && resolvedRefsFromRoots === 0` (the `routing_unresolved` state; `root_absent` is excluded and keeps floor-to-0), floor-to-0 preserved when ≥1 edge resolves (`planning/designs/2026-08-20_agents-md-router-recognition-design.md` D3). `MIN_FILES` **number still open**; fix has **landed**, re-assess after the nine-repo re-run. **Run-6 (2026-08-24): best data yet, still not decided.** Significant-dir counts at `MIN_FILES=5` now span **0 → 1 → 35 → 50 → 240 → 349 → 411 → 1 819** (one-skill/icm 0, superpowers 1, caveman 35, claude-mem 50, cal.com 240, Ghost 349, superset 411, posthog 1 819) — the genuine spread run-2 called too thin. Two sharp signals: **(a)** superpowers still scores `coverage 100` off a **single** significant dir — and it is a *test* dir (`tests/brainstorm-server`) — the strongest argument yet that `5` is too low or that the significance basis must weight source above test dirs; **(b)** posthog's 1 819 significant dirs yield `coverage 87`, the sample's highest from its largest surface — whether that is true or an ancestor-coverage artifact over a deep tree is unanswered. **D3 guard validated:** claude-mem's `coverage 0/n50` catch survived (1 router resolving ≥1 edge → floor-to-0 preserved, not softened to `null`). **No number set.** **`/decisions` 2026-08-24 — `MIN_FILES` REMAINS DEFERRED, not forked.** TBD-12 was not one of the four run-6 forks: run-6 sharpened the question but resolved nothing, and no decision record was opened for it. `coverage` is untouched by the TBD-16 and TBD-14 rulings — its directory-granular basis is correct and is not implicated in either — which is why it is currently the **only** routing-layer sub-score eligible to carry the headline (see TBD-10). That makes settling `MIN_FILES` more load-bearing than before, not less. **No number set.** |
| TBD-13 | `context_audit` **`broken_ref` signal quality** — illustrative/example markdown links inside prose or teaching docs (e.g. `[FORMS.md](FORMS.md)` in a skill-authoring guide) are literally unresolvable and count as `broken_ref`, inflating the rate with non-actionable findings. Should `broken_ref` weight (or scope) links in routing/context docs above example links in arbitrary prose, and by what rule? | `broken_ref` finding precision + calibration interpretation (not the resolver — the resolver base is correct) | Open | Surfaced 2026-08-20 by the routing census: superpowers reported 24 `broken_ref` over 37 edges, but **15 of 24 came from one teaching doc** (`skills/writing-skills/anthropic-best-practices.md`) citing example filenames. Same class as B-A-MCP's own plan-doc `[a](./a.md)` examples in calibration run-1 — it will recur on **every repo that documents markdown**. The spot-check confirmed the resolver joins to the containing file's directory and handles `..` correctly, so this is a **precision/scope** question, **not** a resolver bug. Stub: leave `broken_ref` behavior as-is; do not solve during the parser-gap fix. Candidate directions (not chosen): confine `broken_ref` to routing/context docs; or a "fenced/example link" heuristic; or a severity split. Needs calibration data across the post-parser-fix runs before resolving. **RESOLVED (policy) 2026-08-20** — see `planning/decisions/2026-08-20_tbd-13-broken-refs-router-docs-only.md`: **only broken links in ROUTER docs (`isRoot`) count toward the score**; non-router broken links become `info` findings (reported, unscored). In the code model this = demote `broken_ref` to `info` and **REMOVE the `broken_refs` sub-score entirely** — **four sub-scores replace five** (`bloat`, `orphans`, `routing_drift`, `coverage`), because router-doc broken links are already exactly `routing_drift` (same numerator/source; run-2 confirmed scored router broken links = 0 on all five). Output-contract change (schema + `src/API.md` same commit). See `planning/decisions/2026-08-20_broken-refs-removed-four-subscores.md`. Implementation deferred to the code pass; weighting folds into TBD-10. No number set. |
| TBD-14 | `context_audit` **orphan scope** — can `orphans` distinguish a broken routing graph from a repo's layout choices, without reading source? | `orphans` sub-score; **blocks orphans carrying weight in TBD-10** | Open | Surfaced 2026-08-20 by the run-4 audit of `superpowers`: `orphans 0/n61`, but all 61 (verified on disk) are **structural, not rot** — **43 under `docs/`** (19 dated plan docs under `*/plans/`, 24 other archival/design docs) + **18 under `skills/*/`** (skill support files loaded by directory convention from each `SKILL.md`, never linked from a router). So `orphans` scored a **layout choice**, and its 0 is an artifact. Open: should orphan scope exclude convention-discovered files and dated archival dirs, and how are they identified without inspecting source? Evidence + bucket counts: `planning/decisions/2026-08-20_orphan-scope-layout-vs-rot.md`. Do **not** fix on one repo; confirm across the four approved app repos first. **Run-6 (2026-08-24): ADVANCED — the app-repo confirmation now exists.** With the real routers visible, `orphans` fires on all four app repos (it was `null` on 3/4 in run-5): posthog **659/n678**, cal.com 175/n218, superset **113/n113**, Ghost 11/n36. **superset is the new decisive case:** score **0** with *every* candidate orphaned, while `coverage` reads **73** on the same repo — its 2 routers resolve 26 edges, but to **directories**, so no document is ever directly referenced. That is `orphans` scoring a routing *style* (route-to-directory), a second layout-vs-rot class alongside superpowers' convention-discovered skill files. Still **not resolved** — but the evidence base is no longer one repo. **`/decisions` 2026-08-24 — CAUSE IDENTIFIED; fix authorised as its own loop; TBD-14 stays Open.** See `planning/decisions/2026-08-24_orphans-routes-to-dirs-not-docs.md`. The 2026-08-20 record's do-not-fix-on-one-repo condition is now **satisfied** (4 app repos confirm). The question **changes shape**: not "which files should orphan scope exclude" (scope) but **"at what granularity is reachability computed, and how far does a directory route propagate"** (granularity). superset is decisive — 2 routers resolve **26** edges and `coverage` reads **73**, yet **113/113** documents are orphaned, because its routers route to **directories** and reachability is document-granular. Excluding file classes would not have fixed it: those 113 are ordinary docs under directories that **are** routed. **Directory-granularity reachability authorised as its own build loop**; the propagation-depth question (directory only / immediate children / subtree) is where a new false-negative class could be created and is **not chosen here**. The 2026-08-20 convention-discovered/archival question is **not closed** and carries into the same loop as the secondary class. **`orphans` stays out of TBD-10 weighting until that loop lands and is re-validated** — an exclusion pending a correctness fix, parallel to TBD-16's correctness-null, **not** a weighting decision. |
| TBD-15 | `context_audit` v1.1 AGENTS.md-only follow-ups (deferred from the AGENTS.md router-recognition build, `planning/decisions/2026-08-20_agents-md-router-recognition.md`): (a) an `AGENTS.md`-only repo (no `CLAUDE.md` entry) reports `root.method: "claude_md"` — a distinct `agents_md` method value is the clean fix; (b) an empty/content-free `AGENTS.md`-only root surfaces only as `routing_unresolved` (info), not `root_empty` (critical), because the `root_empty` probe in `index.ts` matches the literal `claude.md` filename — the probe should key off the anchoring router doc regardless of name. Both share the same root cause (the accepted `root.method` label limitation) and should be resolved together. | v1.1 polish — nothing blocked; current behavior is non-silent (empty root shows info finding + null headline) | Open | |
| TBD-16 | `context_audit` **`routing_drift` precision** — a path-shaped backtick in router prose is resolved against **the router's own directory**, but router prose very often describes a *different* directory. What actually qualifies as a route, versus a path mentioned in prose? | **Blocks TBD-10** (`routing_drift` carries the joint-highest stub weight, `3`); `routing_drift` sub-score credibility; the README sample | Open (**opened 2026-08-24 by run-6**) | Evidence: `planning/calibration/2026-08-24_context-audit-run-6-nine-repo-rerun.md` §3. All **59** drift findings across the four repos that produce any were checked against the filesystem: **26 prose-relative false positives** (target exists, one directory deeper — e.g. posthog's `products/signals/skills/AGENTS.md` says "the generalist keeps `references/conventions.md`", which resolves to a missing sibling path while `signals-scout-general/references/conventions.md` **exists**), **10 placeholder tokens** (`<chart_id>`, `<dir>`, `foo`), leaving **~10 plausibly genuine → precision ≈ 17 %**. Three named sub-classes: **prose-relative** (dominant), **install-target** (caveman routers describing where files land in the *consumer's* repo — `.windsurf/rules/`, `.clinerules/`, `.github/copilot-instructions.md`), and **bare-literal** (`.md` alone; posthog `products/stamphog/AGENTS.md:53` + caveman). **This ABSORBS the former standalone "caveman-28 drift residue"** — it was never caveman-specific; posthog reproduces it independently, satisfying the do-not-fix-on-one-repo rule. **Also note:** broken **markdown** links in routers produced their first-ever hits this run (posthog, 2) and **both were template placeholders** (`chart:<chart_id>`, `chart:<id>`) — md-link drift has still never fired on a genuine defect across nine repos and six runs. Same *shape* as TBD-13 (illustrative links inflating `broken_ref`) but scored, not demoted to info. Per task-observer obs #10, treat as a **definition** problem (tighten what counts as a route, and write it AS the definition), not a filter bolted on top. **`/decisions` 2026-08-24 — RESOLVED (policy + shape); fix authorised as its own build loop; TBD-16 stays Open until it lands.** See `planning/decisions/2026-08-24_routing-drift-precision-and-interim-disposition.md`. **(D1) Scoped as PRECISION + DISPLAY, not a weighting question** — at zero weight the ~49 wrong flags still render, so precision cannot be parked behind TBD-10. **(D2) `routing_drift` contributes `null` to the headline — a CORRECTNESS-driven null, not a data null.** Every other `null` here means "empty population/basis"; this one means **"the measurement is not trusted."** It is **NOT** resolved by assigning a weight later. **Restore trigger: the base-dir precision fix landing AND being re-validated against the nine-repo corpus.** Reuses the existing null-drop + weight-renormalisation + §5 guard untouched; **no weight-0 path invented** (weight 0 was measured and rejected — icm-architect 23 → **90**, hygiene-only `bloat` carrying the headline). **(D3) `routing_path_missing` demoted to `info` — INTERIM and REVERSIBLE**, precedent TBD-13's `broken_ref` move: still reported, unscored, visibly lower-confidence, so the ~10 genuine catches stay visible while the ~49 stop reading as high-severity. **`info` is NOT its permanent home — restore trigger: on the precision fix landing and being re-validated, `routing_path_missing` returns to `high`.** Suppressing the category entirely was **rejected** (would abandon the ~10 real broken routes and recreate the pre-2026-08-20 "structurally incapable of firing" state). D2 and D3 are **paired and revert together** on the same fix. **(D4)** caveman-28 **absorbed** — posthog reproduces every sub-class, so it was never caveman-specific. **(D5)** The base-dir over-read fix **and** the md-link-drift disposition (0 genuine findings in 9 repos / 6 runs) are **authorised as a separate build loop** — treat as a **definition** problem, not a filter; note that "add the described directory as a third base" is *not* obviously right (needs prose semantics; risks accidental matches + a new false-negative class). Output-contract-visible: `src/API.md` + `SEVERITY_BY_CATEGORY` update in the same commit as **that** code change (rule 8), **not** in the docs pass. **No number set.** |
| TBD-17 | `context_audit` **further router conventions** — three routing conventions the parser does not read were found by run-6's pre-flight. Should any be added in v1? | Router-name set / link parser; recorded so a future session does not re-discover and guess | **Resolved** | Evidence: run-6 §6 — **all three were measured, not assumed, and all three are low-yield** (unlike `AGENTS.md`, none is a v1 STOP). **(a) `@`-import lines** (`@./path`) — the Claude Code / Gemini CLI context-import syntax, arguably the strongest possible routing edge, but across all nine repos recognized routers hold only **7** genuine `@`-import edges (posthog 3, caveman 4) against caveman's 385 and posthog's 431 resolved refs. Parsing is also **hazardous**: a naive `^@` rule eats Python decorators — superset's `mcp_service/CLAUDE.md` alone has 11 `@tool(`/`@pytest.fixture` lines in code fences, posthog has `@action(...)` — so any rule must exclude fenced blocks and require a path shape. **(b) `GEMINI.md`** — present 3/9, but superset's is a **symlink → `AGENTS.md`** (D2 correctly deduped it, and a `GPT.md → AGENTS.md` symlink alongside it); the two real files are 92 B and 131 B holding only `@`-imports to skills already routed elsewhere → ≈ zero added signal. **(c) `Ghost/CONTEXT-MAP.md`** — a genuine hub router, but the three `CONTEXT.md` files it lists are **already found directly**, so it would add 3 edges to Ghost's 34; 1/9 repos. **RESOLVED `/decisions` 2026-08-24 — add no new router filename and no new link syntax in v1.** See `planning/decisions/2026-08-24_tbd-17-no-new-router-syntax-v1.md`. `AGENTS.md`/`CLAUDE.md`/`CONTEXT.md` remain the complete router-name set; markdown links + backtick code-span paths remain the complete edge syntax. This is **rule 7 / D7 applied to measured evidence, not to a guess** — D7 set the condition ("until a run shows the population using it"); run-6 is that run and the answer is **no**: the population does carry these conventions, at a yield that does not justify the parse risk. The bar is the one `AGENTS.md` cleared (root layer invisible on 4/4, every sub-score an artifact); none of the three is close. **`@`-imports additionally require fenced-code awareness the parser does not have** (`2026-08-20_backtick-routing-edges-and-orphans-guard.md` §73 records that fenced blocks are not parsed specially). **Reopens only if** a future run shows a convention carrying a **material share** of routing edges **and** parseable without a new false-positive class. **No code change; nothing to update in `src/API.md`.** Also recorded: superset ships **three** root aliases to `AGENTS.md` (`CLAUDE.md`, `GEMINI.md`, `GPT.md`) and D2 correctly emitted no `symlink` finding for any; superpowers ships `AGENTS.md → CLAUDE.md`, the reverse direction, also deduped correctly. |

> **TBD policy:** If a TBD blocks implementation, stub it, leave a `TODO: TBD-XXX` comment, and continue. Do not guess. When a TBD is resolved, record the reasoning in `planning/decisions/YYYY-MM-DD_title.md`.
