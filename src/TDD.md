# TDD.md — Technical Design Decisions

> **TBD policy.** When you encounter a TBD reference in any document, look it up in the Master TBD Tracker below. If status is **open**: stub it, leave a `TODO: TBD-XXX` comment, and continue. **Do not guess** at the resolution. This file lives in `src/`, beside the code it binds to.

Stack and implementation conventions for this server — TypeScript ESM, `NodeNext`, `node:test`, the `index.ts`/`server.ts` split, the structured error envelope, and the context-budget ledger — live in [`src/CONTEXT.md`](CONTEXT.md). This file holds one thing: the canonical TBD tracker.

---

## Master TBD Tracker

This is the canonical TBD log. **All TBDs from all other documents resolve here.**

When a TBD is resolved:
1. Update **Status** to `Resolved` and fill in **Resolution**.
2. Record the reasoning in `planning/decisions/YYYY-MM-DD_title.md` (this tracker holds the status; the decision record holds the why).
3. Remove it from `CLAUDE.md`'s Key Open TBDs table.
4. Update any document that referenced it.
5. Build the feature that was blocked.

| TBD ID | Description | Blocks | Status | Resolution |
|---|---|---|---|---|
| TBD-1 | Does claude-mem's upstream repo ship an Apache 2.0 `NOTICE` file? | `THIRD_PARTY_NOTICES.md` — Apache 2.0 compliance | **Resolved** | **YES** — a `NOTICE` (242 B, `Copyright 2026 Alex Newman`) exists at repo root (`e2d1df5`); Apache-2.0 §4(d) requires reproducing its contents in the claude-mem notice block. See `planning/decisions/2026-08-20_tbd-1-claude-mem-notice.md`. |
| TBD-2 | Confirm each of the **five** bundled components' license from its own `LICENSE`/`plugin.json` (now includes `icm-architect`), not third-party listings | All packaging; `THIRD_PARTY_NOTICES.md`, `Integration_Spec.md` | **Resolved 2026-08-27** | All five re-read live from their own files at the pinned commits: superpowers MIT (`b36e082`, Jesse Vincent), caveman MIT scoped to `skills/caveman/` (commit `a42ef76`, Julius Brussee — engine dirs BSL-1.1 excluded per `LICENSING.md`), claude-mem Apache-2.0 (`e2d1df5`, Alex Newman, ships `NOTICE`), task-observer CC-BY-4.0 (`281f134`, holder Eoghan Henn / rebelytics.com from README), icm-architect MIT (`b20fb45`, **Jake Van Clief** — "RinDig" was the org, corrected). **caveman sub-item resolved:** pin the **commit `a42ef76`** (package.json 2.2.0 ≠ tag pi-v0.1.0); notice path-scopes to `skills/caveman/`, does not imply whole-repo MIT. Notices file finalized (STUB removed). See `planning/decisions/2026-08-27_tbd-2-4-resolved.md`. |
| TBD-3 | Verify DO Functions free allowance against the live console (25,000 vs 90,000 GiB-s) | Cost model only — not load-bearing | **Resolved 2026-08-27** | **90,000 GiB-s/month per account** — confirmed from DO's own live pricing page (`digitalocean.com/pricing/functions`): overage $0.0000185/GiB-s, 100ms min runtime; DO's own example ≈ 3.6M free invocations/mo at 256 MiB × 100ms. The 25,000 figure was a stale third-party tracker; DO docs win. Conclusion unchanged and never load-bearing — the allowance is not a constraint at any realistic volume. A one-time live-console spot-check stays on the pre-Phase-2 ops checklist. |
| TBD-4 | Do B&A docs reproduce ICM (Van Clief & McDermott, 2026) expression, or paraphrase it? **Escalated:** `icm-architect` is a 905-star MIT expression of the same paper, now bundled — resolve before the notices file ships and before any copy claims the methodology as B&A-original | Publication | **Resolved 2026-08-27** | Owner ruling: B&A **reproduces** expression — from the bundled **`icm-architect` files** (Van Clief, MIT), **not** the paper's text. Covered by the icm-architect MIT notice + an explicit reproduction/attribution statement (copyright + license text retained). No separate paper-license dependency. Repo scan found **no** "B&A-original methodology" copy to correct (docs distinguish governance-vs-generation, they don't claim originality) — future such copy would reopen this. See `planning/decisions/2026-08-27_tbd-2-4-resolved.md`. |
| TBD-5 | Paid-tier price and structure (one-time vs. subscription) | `export_record` checkout | **Resolved 2026-08-27** | Owner ruling: **$9/month subscription.** This settles the auth semantics for `export_record` — validation must check **entitlement** (is the subscription currently active in Stripe?), not merely one-time key validity. Feeds the Integration_Spec §1 contract (`Auth`) + the Phase-2 checkout/key-issuance work in the site repo. See `planning/decisions/2026-08-27_tbd-5-8-pricing-and-launch.md`. |
| TBD-6 | Package/repo name | Everything — nothing scaffolds unnamed | **Resolved** | Repo `B-A-MCP`; npm package `b-a-mcp`. Recorded in `planning/decisions/`. |
| TBD-7 | Pinned Superpowers major version | Dependency stability | **Resolved** | Pin **`v6.3.0`** commit **`b36e082`** (MIT, `Copyright (c) 2025 Jesse Vincent`, upstream `github.com/obra/superpowers`). Pin the commit, not the tag. See `planning/decisions/2026-08-20_tbd-7-superpowers-pin.md`. |
| TBD-8 | Split launch (free tier first) or single launch? | Sequencing | **Resolved 2026-08-27** | Owner ruling: **split launch — free tier first.** Ship the keyless free tier (three tools + five gates) to npm now; the paid `export_record` ships in a later Phase-2 launch once the site-repo checkout is built. Consistent with the roadmap's phase order; the free tier is the lead-gen/authority asset and does not wait on the paid backend. See `planning/decisions/2026-08-27_tbd-5-8-pricing-and-launch.md`. |
| TBD-9 | `doc_drift` scope — which frameworks and migration formats are in scope for v1 | `doc_drift` build | **Resolved** | **v1 = provided/inline structural schema-drift differ** over a caller-supplied `{declared, canonical}` schema pair (input is an array of pairs → one consolidated report, mirroring `override_log`'s array→log). Detects three drift kinds — field-only-in-doc (stale doc), field-only-in-canonical (undocumented), type/shape mismatch — with a rounded 0–100 drift score (null on empty), findings, stats, and a tool-built `rendered` report. **Opaque `{type:object}`-no-`properties` nodes are wildcards** (D9, written as the definition): they match any counterpart, so no drift is emitted or detected inside — the accepted silent-FN limitation whose documented recourse is D10 (feed a more complete canonical). Keyless, read-only, **file-free** (mirrors `override_log`, not `context_audit`'s file walk); the impure discovery step (obtain the canonical) is relocated to the calling agent. **Diff-sides = provided/inline; read-files rejected** by Findings 1–2 (B-A-MCP emits no committed canonical artifact — schemas are TS literals; and its doc deliberately diverges from the minimized emitted schema). **OUT, stubbed:** canonical self-discovery/read-files (**TBD-22**), orientation walk-test (**TBD-23**), framework migration/route parsing (**TBD-24**). Decision + full ledger: `planning/decisions/2026-08-27_doc-drift-v1-scope-and-diff-sides.md`. |
| TBD-10 | `context_audit` sub-score → headline **weighting** function (accuracy cluster > bloat; N/A sub-score drops and reweights). | `context_audit` composite `score` | Open | **Principle resolved** (accuracy > bloat). **Structural prerequisite RESOLVED 2026-08-20** — every sub-score now carries `n` and an empty denominator reports `null` ("not assessed"), never a misleading 100 that would weight like a verified check; see `planning/decisions/2026-08-20_subscore-confidence-signal.md`. **Weight NUMBERS remain open, data-blocked.** Current stub `TBD_10_WEIGHTS` (`src/tools/context-audit/score.ts`): `broken_refs:3, routing_drift:3, orphans:2, coverage:2, bloat:1`. **Do NOT resolve from B-A-MCP's run alone** (backtick routing → degenerate `refsFromRoots`/`refsFromNonRoots` denominators); needs ≥2–3 hyperlink-routed calibration repos. **Run-2 (2026-08-20, `planning/calibration/2026-08-20_context-audit-run-2-post-parser-fix.md`): still DEFERRED.** Now weighting ranges over **four** sub-scores, not five — `broken_refs` is removed (`planning/decisions/2026-08-20_broken-refs-removed-four-subscores.md`). `routing_drift` was non-discriminating (100 everywhere) until the router-path-drift rule (`planning/decisions/2026-08-20_router-path-drift.md`) gave it teeth; re-assess its weight after run-3. **Run-5 (2026-08-20, app sample) produced NO valid drift data** — all four app repos route via a root `AGENTS.md` the tool did not recognize (invisible routers). The fix has **landed** (`planning/designs/2026-08-20_agents-md-router-recognition-design.md`); numbers now gated on the nine-repo re-run only. **Run-6 (2026-08-24, the nine-repo re-run, `planning/calibration/2026-08-24_context-audit-run-6-nine-repo-rerun.md`): the `AGENTS.md` gate is CLEARED but TBD-10 is RE-BLOCKED for a new reason.** `routing_drift` now fires at volume (posthog n431, caveman n385) — but a filesystem check of all 59 drift findings found precision ≈ **17 %** (26/59 prose-relative false positives, 10 placeholder tokens, ~10 plausibly genuine). It carries the joint-highest stub weight (`3`); weighting a sub-score that measures prose is the same error class as the rejected `6000`. **Now blocked on TBD-16.** **`/decisions` 2026-08-24 — WEIGHT NUMBERS REMAIN DEFERRED, and the eligible sub-score set has narrowed.** Two of the four sub-scores are now **excluded from the composite pending correctness fixes**, neither exclusion being a weighting decision: **(a)** `routing_drift` contributes a **correctness-driven `null`** until its precision fix lands and is re-validated (`planning/decisions/2026-08-24_routing-drift-precision-and-interim-disposition.md` D2) — a weight of `0` was explicitly **rejected** because `ROUTING_LAYER_KEYS` is separate from `TBD_10_WEIGHTS`, so a zero-weighted drift still satisfies the §5 headline guard while contributing nothing, leaving hygiene-only `bloat` to carry the headline (measured: icm-architect 23 → **90**); **(b)** `orphans` stays out of the weighting until directory-granularity reachability lands (`planning/decisions/2026-08-24_orphans-routes-to-dirs-not-docs.md` D3). With `bloat`'s aggregation also shape-blocked, **`coverage` is currently the only routing-layer sub-score eligible to carry the headline** — a narrowness to watch, not a reason to relax either guard (§5 holds: a `coverage` measurement, floor of 0 included, is a routing-layer measurement). **`TBD_10_WEIGHTS` / `ROUTING_LAYER_KEYS` in `src/tools/context-audit/score.ts` are NOT edited by any of this.** **Update 2026-08-25 — TBD-16 CLOSED, so `routing_drift`'s correctness-null is LIFTED and it is now WEIGHTING-ELIGIBLE** (`planning/calibration/2026-08-25_context-audit-tbd-16-revalidation-run2.md`). The weight **NUMBER stays deferred** (still data-blocked; no number set). `orphans` remains excluded pending TBD-14 (dir-granularity); with `bloat` shape-blocked pending TBD-11, **`coverage` remains the sole load-bearing headline routing-layer sub-score** until those two loops land. `TBD_10_WEIGHTS`/`ROUTING_LAYER_KEYS` unchanged. **Update 2026-08-26 (run-7, `planning/calibration/2026-08-26_context-audit-run-7-numbers-calibration.md`): the two loops (TBD-11 shape, TBD-14) HAVE landed; run-7 is the first consolidated four-sub-score snapshot on shape-clean `main` — and it surfaces TWO weighting blockers, so TBD-10 stays Open with a SHARPENED question, not resolved.** §4.1 **`routing_drift` is saturated at ~100** across the corpus (100/n26, 100/n406, 100/n17, 100/n34, 97/n368 on the five meaningful-denominator repos; only tiny-denom superpowers 75/n4 and icm 0/n1 deviate) — post-TBD-16 it is high-precision, near-zero-variance, so weighting it *joint-highest (3)* just pulls headlines toward 100 and dilutes the discriminating signals (the run-6 error class, mirrored: the precision fix removed the FPs and with them the variance a high weight assumes). §4.2 **the `orphans` raw sub-score is layout-dominated** — of TBD-14's 1 077 residual orphans only **20 are genuine-abandoned**; the sub-score (`1 − orphaned/candidates`) counts all candidates, so low `orphans` scores (superset 1/n113, claude-mem 0/n61) measure repo *layout style*, not rot → **spawns TBD-18** (orphans sub-score basis). `coverage` (0→36→73→79→87→89→100) and `bloat` (55→60→80→100, post-TBD-11) discriminate cleanly and are weightable now. Candidate directions (NONE chosen, `/decisions` input): down-weight/condition `routing_drift` for its floor-catching role; re-base or bound `orphans` (gated on TBD-18); set `coverage`/`bloat` first. **No weight NUMBER set (rule 7); owner ratifies at the next interactive gate.** `TBD_10_WEIGHTS`/`ROUTING_LAYER_KEYS` untouched. **PARTIALLY RESOLVED + BUILT 2026-08-26 (`/decisions`, owner-ratified — `planning/decisions/2026-08-26_tbd-10-weights-partial-and-tbd-18-orphans-rebase.md`):** `TBD_10_WEIGHTS = {routing_drift:1, coverage:3, bloat:1}` — `coverage:3` (best discriminator), `bloat:1` (hygiene), `routing_drift:1` (**down-weighted from 3** — saturated at ~100, floor-catcher not driver). **`orphans` weight NOT set — gated on TBD-18** (removed from the weights map; still reported in `subscores`). `ROUTING_LAYER_KEYS → ["routing_drift","coverage"]` (orphans removed — an unweighted orphans must not let the guard pass into a bloat-only headline). Built under TDD (+3 tests → 115); `src/API.md` updated same commit (rule 8: orphans reported-but-unweighted, guard is drift/coverage); schema/ledger unchanged. Headlines recompute (superset 66→80, claude-mem→40). **TBD-10 stays Open for the `orphans` weight ONLY**, gated on TBD-18. **Update 2026-08-26 — TBD-18 CLOSED (TBD-19 + TBD-20 resolved, re-validation SATISFIED: `planning/calibration/2026-08-26_context-audit-tbd-19-20-revalidation.md`), so the `orphans` sub-score exclusion is LIFTED and `orphans` is now WEIGHTING-ELIGIBLE.** All four sub-scores are now eligible. The `orphans` weight **NUMBER stays deferred** (data-blocked; no number set) — its raw sub-score still carries §4-gap noise (component-manifest ~110, test-fixture ~17, bare `docs/**`, each its own future `/decisions` item), so the weight must be set with that residual in mind. `TBD_10_WEIGHTS` (`{routing_drift:1, coverage:3, bloat:1}`) and `ROUTING_LAYER_KEYS` (`["routing_drift","coverage"]`) are **NOT** edited by this — adding `orphans` to the weights map is the next `/decisions` (TBD-10) at an interactive gate. **TBD-10 stays Open for the `orphans` weight NUMBER only.** **RESOLVED (provisional) 2026-08-26 (`/decisions`, owner-ratified — `planning/decisions/2026-08-26_tbd-10-orphans-weight.md`): `orphans:1` added to `TBD_10_WEIGHTS` → `{routing_drift:1, coverage:3, bloat:1, orphans:1}`; `"orphans"` added to `ROUTING_LAYER_KEYS` → `["routing_drift","coverage","orphans"]`.** orphans discriminates (corpus 31–92) so it earns weight; weight 1 (parity with drift/bloat, below coverage:3) is the largest that keeps the §4-gap downward-bias headline pull bounded (~±8). Owner recorded it **provisional — gated on §4-gap detection** (component-manifest / test-fixture / bare-`docs/**`, design §9): the ceiling is §4-gap detection, not signal quality; `orphans` is **raise-eligible** when those three `/decisions` items land. Rejected `orphans:2` (pushes unfixed bias into the headline) and defer (undersells a discriminating signal). Built under TDD (score.ts + 4 headline tests → 131/131); `src/API.md` updated same commit (rule 8 — score/subscores/guard descriptions); ledger 252 unchanged (rule 2); no schema change. Headlines recompute (superset 80→72, caveman 89→83, claude-mem 40→45). **TBD-10 stays Open ONLY for the provisional→final `orphans` raise**, gated on the three §4-gap items; `coverage:3`/`bloat:1`/`routing_drift:1` are final. |
| TBD-11 | `context_audit` **bloat thresholds** — routing-token-weight, inline-ratio, chain-depth cutoffs → severity. | `context_audit` `bloat` sub-score | Resolved | Data-blocked — needs more calibration repos (run-1 against B-A-MCP alone is insufficient). Stub constants live in **`src/tools/context-audit/bloat.ts`** — **read them from source, do not trust a value quoted here** (corrected 2026-08-24: this row previously named `TBD_11_ROUTING_TOKEN_CUTOFF=4000` and `TBD_11_DEPTH_CUTOFF=4`, neither of which exists any more; the per-router reshape renamed them). Values as of 2026-08-24: `TBD_11_ROUTER_TOKEN_CUTOFF=3000`, `TBD_11_CHAIN_TOKEN_CUTOFF=6000`, `TBD_11_CHAIN_DEPTH_CUTOFF=4`, `TBD_11_INLINE_RATIO_CUTOFF=0.85`, `TBD_11_INLINE_MIN_TOKENS=200`. **`TBD_11_CHAIN_TOKEN_CUTOFF=6000` is NOT the rejected `6000`** — the rejected number was a flat-total routing-token cutoff; this one is a root→leaf chain sum, a different metric. The rejection below stands. **`ROUTING_TOKEN_CUTOFF=6000` was CONSIDERED and REJECTED (2026-08-20):** the calibration-run-1 doc proposed raising 4000→6000, but that 6000 was chosen to stop the tool flagging **its own repo's** routing layer (B-A-MCP measures 4619 routing tokens) — tautological self-tuning (the repo is not a neutral baseline), and a single repo cannot constrain the cutoff's top end, which needs a repo with a genuinely bloated (10k+ token) router. **Stub stays 4000** until such a repo is calibrated. Inline-ratio/min-tokens/depth: no contrast data on B-A-MCP → unchanged. **SHAPE RESOLVED 2026-08-20** — see `planning/decisions/2026-08-20_tbd-11-bloat-per-router-not-total.md`: bloat is **per-router + per root→leaf chain**, NOT a flat total (run-2's caveman — 45,419 tokens / 23 routers ≈ <2,000 each — showed a flat total fails a healthy large monorepo). Per-router size needs no new data (walk already yields it); the **chain sum needs the router→router adjacency exposed from `buildGraph` into `scoreBloat`** (walk does not produce edges; the current `depth` proxy is filesystem-path depth, not chain depth). Cutoff NUMBERS remain open, data-blocked; implementation deferred. **Run-5 (2026-08-20):** posthog's ≈9 897-token root `AGENTS.md` + 44-file `AGENTS.md` tree is a real top-end candidate. The fix has **landed** (`planning/designs/2026-08-20_agents-md-router-recognition-design.md`); posthog's root `AGENTS.md` + tree is now measurable — capture it in the nine-repo re-run. **Run-6 (2026-08-24): top-end data UNBLOCKED, shape RE-BLOCKED.** Real datapoints at last — posthog 45 routers / 155 280 routing tokens / 9 834-token root `AGENTS.md`; cal.com's 15 125-token `agents/skills/vercel-react-best-practices/AGENTS.md`; first-ever `chain_depth` fire (posthog, 5). But two **shape** defects must be fixed before any cutoff is set: **(A)** the penalty is a flat sum over **all** routers with no normalization, so router *count* drives the score to the floor — caveman (40 routers, mean **1 651** tokens/router) scores **0/n40** while superpowers (1 router, **2 211** tokens) scores **90/n1**, and superset (mean 5 082) scores 45. The 2026-08-20 per-router decision moved the *metric* per-router but left the *aggregation* a flat sum — run-2's original complaint, one level up. **(B)** `INLINE_RATIO_CUTOFF=0.85` is a constant, not a discriminator: it produces **86 of ~117** bloat findings and fires on **8/9** repos, including a **262-token** `claude-mem/CLAUDE.md` (ratio 0.86) and superpowers' reference-quality router (0.98); `INLINE_MIN_TOKENS=200` is far too low. `router_tokens`/`chain_tokens`/`chain_depth` DO discriminate (26/100 routers over the size cutoff) and are the metrics worth weighting. **No cutoff set.** Route the shape fix through `/decisions` first. **`/decisions` 2026-08-24 — RESOLVED (shape finding + authorisation); CUTOFF NUMBERS REMAIN DEFERRED.** See `planning/decisions/2026-08-24_tbd-11-bloat-aggregation-shape.md`: the 2026-08-20 per-router ruling stands and is **not** re-litigated, but the implementation only half-satisfies it — the *metric* moved per-router while the *penalty aggregation* stayed a flat sum over every router, so router **count** drives the floor. A **shape re-work is authorised as its own build loop** (how per-router and per-chain penalties combine into one bounded sub-score without count dominating — normalisation / worst-case / per-chain-only all candidates, **none chosen**). `INLINE_RATIO_CUTOFF` **flagged for re-derivation** in that same loop (whether the metric survives at all is part of it). The data blocker is cleared; a **shape** blocker replaces it. **No number set or re-derived.** **`/decisions` + BUILT 2026-08-26 — aggregation SHAPE RESOLVED and implemented; SHAPE BLOCKER LIFTED; NUMBERS STILL DEFERRED.** See `planning/decisions/2026-08-26_tbd-11-bloat-worst-case-aggregation.md` + design `planning/designs/2026-08-26_bloat-worst-case-aggregation-design.md`. Chosen shape: **worst-case over the heaviest root→leaf chain** (candidate `worst-case`; `mean` rejected — dilutes a catastrophic router; `per-chain-only` rejected on the house tie-breaker — silent on a mid-chain giant). `scoreBloat` reworked: the per-router size term is taken **MAX over routers, never summed** (router *count* can no longer drive the score to the floor — the run-6 caveman `0/n40` defect); the two **token** terms combine by `max(chain_token_term, max_router_term)` (NOT sum — the heaviest router usually sits on the worst chain, so summing double-counts); **depth ADDS** (a separate axis — hops, not tokens). **`inline_ratio` DROPPED entirely** — metric, finding, and the `TBD_11_INLINE_RATIO_CUTOFF`/`TBD_11_INLINE_MIN_TOKENS` constants are removed (broken by construction: "mostly prose + a few paths" describes what a router *is*; it fired 86/117 findings on 8/9 repos). **Live constants now (read from `src/tools/context-audit/bloat.ts`, do not trust this row): `TBD_11_ROUTER_TOKEN_CUTOFF=3000`, `TBD_11_CHAIN_TOKEN_CUTOFF=6000`, `TBD_11_CHAIN_DEPTH_CUTOFF=4`** — the two INLINE constants no longer exist. Findings still enumerate every breaching router/chain (transparency; visible FP > silent FN); only the SUBSCORE switched to worst-case. Tests +5 (→112). `context_audit` JSON schema unchanged (bloat is `{score,n}`) → `src/API.md` untouched (rule 8 not triggered); ledger cost unchanged (rule 2). **Cutoff NUMBERS remain Open and data-blocked** — external hyperlink-routed repos only, never B-A-MCP's own run. **Update 2026-08-26 (run-7, `planning/calibration/2026-08-26_context-audit-run-7-numbers-calibration.md`): the worst-case shape is CONFIRMED in the wild for the first time** (count-domination gone: caveman `0/n40 → 80`, posthog `0/n45 → 55`; healthy routers lifted to 100; caveman is the mid-chain-giant case in the wild — its 7 283-token lone router carries the penalty via `maxRouterTerm`, not its chain). The raw distribution (validated: the harness re-derives every repo's `bloat` sub-score exactly, `bloatMatch: true` ×9) makes the cutoffs assessable: **`ROUTER_TOKEN_CUTOFF=3000`** sits at ~p75 but is **score-neutral** — no repo's heaviest router lands in the contested 3 000–4 000 band, so 3000-vs-4000 changes no `bloat` score in-sample (only finding volume 26→16); the reference-quality router (superpowers 2 211) is safely below and genuine bloat (8 000–22 196) far above → **data-supports keeping 3000** (or 4000 on finding-noise grounds), ratification-ready. **`CHAIN_TOKEN_CUTOFF=6000`** falls inside a clean natural gap in worst-chain tokens (2 211 → [gap] → 7 437) → **data-supported at 6000**, ratification-ready. **`CHAIN_DEPTH_CUTOFF=4` is UNDER-DETERMINED** — only posthog exceeds it (depth 5); every other repo is depth 1–2; **zero observations at depth 3 or 4**, so the corpus cannot distinguish a cutoff of 3/4/5 → keep 4 conservatively, flagged as the one cutoff run-7 does not truly calibrate (a larger/deeper corpus would help only this one). **No cutoff NUMBER set (rule 7); owner ratifies + a TDD build decommissions the two ratified `TODO: TBD-11` stubs (values numerically unchanged, so `src/API.md`/ledger untouched).** **RESOLVED 2026-08-26 (`/decisions`, owner-ratified — `planning/decisions/2026-08-26_tbd-11-tbd-12-cutoff-numbers-ratified.md`):** `TBD_11_ROUTER_TOKEN_CUTOFF=3000` (score-neutral) and `TBD_11_CHAIN_TOKEN_CUTOFF=6000` (clean gap) ratified as decided values; `TBD_11_CHAIN_DEPTH_CUTOFF=4` **kept, flagged under-determined** (only 1 corpus obs >2; corpus can't distinguish 3/4/5). All values numerically unchanged → no behaviour change, no boundary tests. `bloat.ts` `TODO: TBD-11` markers decommissioned (top-of-file + three inline); constants carry ratification rationale. Schema/ledger untouched (rule 2). **TBD-11 CLOSED.** |
| TBD-12 | `context_audit` **coverage significance + thresholds** — source-vs-config classification, min file count for "significant", ancestor-coverage-within-N-hops vs own-router. Data-blocked — calibrate from the first dogfood run. **Build guard:** both the `coverage` (source, high) and `coverage_test` (test dir, medium) "uncovered significant workspace" findings must be gated behind a `TODO: TBD-12` so neither fires on uncalibrated defaults. | `context_audit` `coverage` sub-score | Open | Test-dir-vs-source **severity policy** sub-question RESOLVED — see `planning/decisions/2026-08-20_test-dir-coverage-severity.md` (test dirs stay significant but emit `coverage_test`/medium instead of `coverage`/high). The significance **threshold numbers** remain open, data-blocked (need more repos with varied source layouts; run-1 gives one significant/non-significant boundary pair, too thin). Current stubs (`src/tools/context-audit/coverage.ts`): `TBD_12_MIN_FILES=5`, `TBD_12_SOURCE_EXTS=[.ts, .js, .tsx, .jsx, .py, .go, .rs, .java, .rb]`. **Run-2 (2026-08-20): real layout spread now (significant-dir counts 0 / 1 / 2 / 35 / 50 at MIN_FILES=5); floor-to-0 policy validated (claude-mem coverage 0/n50). Still DEFERRED** — claude-mem's 50 significant dirs from a small repo raises "is MIN_FILES=5 too low?"; the data poses the question, does not answer it. Related invariant ratified: headline is a definite number whenever ≥1 significant dir, null allowed at 0 (`planning/decisions/2026-08-20_headline-definite-when-significant-dirs.md`). **Run-4 thin-coverage datapoint:** superpowers scored `coverage 100/n1` resting on a **single** significant directory — `tests/brainstorm-server`, the only dir with ≥5 source files (a **test** dir). Not a bug, but a coverage of 100 anchored on one test dir is thin and bears directly on the `MIN_FILES` question (is 5 too low/high, and should the significance basis weight source vs test dirs). **Run-5 (2026-08-20):** `coverage` **routing-basis guard RESOLVED** — `coverage` reports `null` when `routing_files > 0 && resolvedRefsFromRoots === 0` (the `routing_unresolved` state; `root_absent` is excluded and keeps floor-to-0), floor-to-0 preserved when ≥1 edge resolves (`planning/designs/2026-08-20_agents-md-router-recognition-design.md` D3). `MIN_FILES` **number still open**; fix has **landed**, re-assess after the nine-repo re-run. **Run-6 (2026-08-24): best data yet, still not decided.** Significant-dir counts at `MIN_FILES=5` now span **0 → 1 → 35 → 50 → 240 → 349 → 411 → 1 819** (one-skill/icm 0, superpowers 1, caveman 35, claude-mem 50, cal.com 240, Ghost 349, superset 411, posthog 1 819) — the genuine spread run-2 called too thin. Two sharp signals: **(a)** superpowers still scores `coverage 100` off a **single** significant dir — and it is a *test* dir (`tests/brainstorm-server`) — the strongest argument yet that `5` is too low or that the significance basis must weight source above test dirs; **(b)** posthog's 1 819 significant dirs yield `coverage 87`, the sample's highest from its largest surface — whether that is true or an ancestor-coverage artifact over a deep tree is unanswered. **D3 guard validated:** claude-mem's `coverage 0/n50` catch survived (1 router resolving ≥1 edge → floor-to-0 preserved, not softened to `null`). **No number set.** **`/decisions` 2026-08-24 — `MIN_FILES` REMAINS DEFERRED, not forked.** TBD-12 was not one of the four run-6 forks: run-6 sharpened the question but resolved nothing, and no decision record was opened for it. `coverage` is untouched by the TBD-16 and TBD-14 rulings — its directory-granular basis is correct and is not implicated in either — which is why it is currently the **only** routing-layer sub-score eligible to carry the headline (see TBD-10). That makes settling `MIN_FILES` more load-bearing than before, not less. **No number set.** **Update 2026-08-26 (run-7, `planning/calibration/2026-08-26_context-audit-run-7-numbers-calibration.md`): NO new data — `coverage.ts` is unchanged since run-6, so run-7 reproduces run-6 EXACTLY** (significant-dir counts 0/0/1/35/50/240/349/411/1 819; superpowers `coverage 100/n1` off a single **test** dir re-confirmed). TBD-12 is therefore **no longer data-blocked** — it is a **policy call on existing evidence** (raise `MIN_FILES`, or weight source above test dirs in the significance basis). The house tie-breaker cuts AGAINST silently raising `MIN_FILES` (a genuine 4-source-file uncovered dir would drop out of scope = silent FN), which is why this is an owner decision, not an obvious bump. Routes to `/decisions` resolve-or-defer at the next interactive gate. **No number set (rule 7).** **`MIN_FILES` RESOLVED 2026-08-26 (`/decisions`, owner-ratified — `planning/decisions/2026-08-26_tbd-11-tbd-12-cutoff-numbers-ratified.md`): `TBD_12_MIN_FILES=5` ratified as a decided value** (value unchanged — raising it drops small-real dirs to silent FN against the tie-breaker; the `/n` denominator already exposes thin anchors like superpowers `100/n1`). `coverage.ts` `MIN_FILES` marker decommissioned. **TBD-12 STAYS OPEN for the rest of its scope** — the source-vs-test significance **basis**, `SOURCE_EXTS` (provisional), and the still-off coverage-finding emission — its own later loop, explicitly not this decision. |
| TBD-13 | `context_audit` **`broken_ref` signal quality** — illustrative/example markdown links inside prose or teaching docs (e.g. `[FORMS.md](FORMS.md)` in a skill-authoring guide) are literally unresolvable and count as `broken_ref`, inflating the rate with non-actionable findings. Should `broken_ref` weight (or scope) links in routing/context docs above example links in arbitrary prose, and by what rule? | `broken_ref` finding precision + calibration interpretation (not the resolver — the resolver base is correct) | **Resolved** | Surfaced 2026-08-20 by the routing census: superpowers reported 24 `broken_ref` over 37 edges, but **15 of 24 came from one teaching doc** (`skills/writing-skills/anthropic-best-practices.md`) citing example filenames. Same class as B-A-MCP's own plan-doc `[a](./a.md)` examples in calibration run-1 — it will recur on **every repo that documents markdown**. The spot-check confirmed the resolver joins to the containing file's directory and handles `..` correctly, so this is a **precision/scope** question, **not** a resolver bug. Stub: leave `broken_ref` behavior as-is; do not solve during the parser-gap fix. Candidate directions (not chosen): confine `broken_ref` to routing/context docs; or a "fenced/example link" heuristic; or a severity split. Needs calibration data across the post-parser-fix runs before resolving. **RESOLVED (policy) 2026-08-20** — see `planning/decisions/2026-08-20_tbd-13-broken-refs-router-docs-only.md`: **only broken links in ROUTER docs (`isRoot`) count toward the score**; non-router broken links become `info` findings (reported, unscored). In the code model this = demote `broken_ref` to `info` and **REMOVE the `broken_refs` sub-score entirely** — **four sub-scores replace five** (`bloat`, `orphans`, `routing_drift`, `coverage`), because router-doc broken links are already exactly `routing_drift` (same numerator/source; run-2 confirmed scored router broken links = 0 on all five). Output-contract change (schema + `src/API.md` same commit). See `planning/decisions/2026-08-20_broken-refs-removed-four-subscores.md`. Implementation deferred to the code pass; weighting folds into TBD-10. No number set. |
| TBD-14 | `context_audit` **orphan scope** — can `orphans` distinguish a broken routing graph from a repo's layout choices, without reading source? | `orphans` sub-score; **blocks orphans carrying weight in TBD-10** | **Resolved** | Surfaced 2026-08-20 by the run-4 audit of `superpowers`: `orphans 0/n61`, but all 61 (verified on disk) are **structural, not rot** — **43 under `docs/`** (19 dated plan docs under `*/plans/`, 24 other archival/design docs) + **18 under `skills/*/`** (skill support files loaded by directory convention from each `SKILL.md`, never linked from a router). So `orphans` scored a **layout choice**, and its 0 is an artifact. Open: should orphan scope exclude convention-discovered files and dated archival dirs, and how are they identified without inspecting source? Evidence + bucket counts: `planning/decisions/2026-08-20_orphan-scope-layout-vs-rot.md`. Do **not** fix on one repo; confirm across the four approved app repos first. **Run-6 (2026-08-24): ADVANCED — the app-repo confirmation now exists.** With the real routers visible, `orphans` fires on all four app repos (it was `null` on 3/4 in run-5): posthog **659/n678**, cal.com 175/n218, superset **113/n113**, Ghost 11/n36. **superset is the new decisive case:** score **0** with *every* candidate orphaned, while `coverage` reads **73** on the same repo — its 2 routers resolve 26 edges, but to **directories**, so no document is ever directly referenced. That is `orphans` scoring a routing *style* (route-to-directory), a second layout-vs-rot class alongside superpowers' convention-discovered skill files. Still **not resolved** — but the evidence base is no longer one repo. **`/decisions` 2026-08-24 — CAUSE IDENTIFIED; fix authorised as its own loop; TBD-14 stays Open.** See `planning/decisions/2026-08-24_orphans-routes-to-dirs-not-docs.md`. The 2026-08-20 record's do-not-fix-on-one-repo condition is now **satisfied** (4 app repos confirm). The question **changes shape**: not "which files should orphan scope exclude" (scope) but **"at what granularity is reachability computed, and how far does a directory route propagate"** (granularity). superset is decisive — 2 routers resolve **26** edges and `coverage` reads **73**, yet **113/113** documents are orphaned, because its routers route to **directories** and reachability is document-granular. Excluding file classes would not have fixed it: those 113 are ordinary docs under directories that **are** routed. **Directory-granularity reachability authorised as its own build loop**; the propagation-depth question (directory only / immediate children / subtree) is where a new false-negative class could be created and is **not chosen here**. The 2026-08-20 convention-discovered/archival question is **not closed** and carries into the same loop as the secondary class. **`orphans` stays out of TBD-10 weighting until that loop lands and is re-validated** — an exclusion pending a correctness fix, parallel to TBD-16's correctness-null, **not** a weighting decision. | **Update 2026-08-25 — DESIGN + reviewed PLAN landed (PR #33), still Open pending execution.** Fully decided: `planning/designs/2026-08-25_directory-granularity-reachability-design.md` (WHAT/WHY), `docs/superpowers/plans/2026-08-25-directory-granularity-reachability.md` (TDD plan, reviewed live twice → CLEAN), `planning/decisions/2026-08-25_tbd-14-root-restricted-dir-propagation.md`. Two settled choices: **depth = directory-only** (subdir of a routed dir stays orphan; full-subtree rejected as masked-rot), **origin = root-restricted** (a directory route propagates only from a REACHED doc, folded into the reachability DFS; flat rejected as silent-FN; T-dir-4/T-dir-5 pin it). Remaining: **TDD build from `main`** (plan Task 0; 102→107) → **categorical re-validation** on the pinned nine-repo corpus (every residual orphan = genuine-abandoned or a named accepted layout class: route-to-directory-nested / convention-discovered / dated-archival) — the **only** thing that closes TBD-14. `orphans` stays out of TBD-10 weighting until then. No number. **Update 2026-08-25 (BUILD LANDED, still Open): code merged (PR #35, `main` `fc3e493`, 102→107, tsc clean).** Directory-only + root-restricted propagation folded into the reachability DFS in `src/tools/context-audit/graph.ts` (per-source `dirTargetsBySrc`, `docsByParentDir`; `routedDirs`/`coverage`/`score`/`index`/`underRoutedDir` untouched); `src/API.md` orphans sentence updated same commit (rule 8); tests T-dir-1…5 landed. Reviewed CLEAN (zero Critical/Important). **TBD-14 does NOT close on this** — the **categorical re-validation** run is the only close condition and is still outstanding (its own later session). `orphans` stays out of TBD-10 weighting until re-val passes. **Update 2026-08-25 (RE-VALIDATION RUN, CLOSE NOT SATISFIED → `/decisions`; TBD-14 stays Open):** `planning/calibration/2026-08-25_context-audit-tbd-14-revalidation.md`. Fix verified **correct** — before/after both fresh this session (pre-TBD-14 `92b74ed` vs `main` `4fee3ae`): total orphans **1 146 → 1 087 (−59)**; posthog −55, caveman −7, cal.com −5, superset/Ghost −1; **zero "should-have-been-reached" failures** (no doc directly inside a reached directory-target left flagged). But §3.4's categorical gate **FAILS**: **162 of 1 087 residuals fit no named accepted class** — three uniform unnamed shapes: **E** runtime-convention config (34: `.claude/agents|rules|commands`, `WARP.md`, `cursor-hooks/`), **F** per-component metadata (110: cal.com `packages/app-store/*/DESCRIPTION.md`), **G** test-fixture markdown (18). Named classes covered A route-to-dir-nested 686 + B convention-discovered(SKILL) 20 + C dated-archival 189 + D genuine-abandoned 20 = 915. Not absorbed into genuine-abandoned (that would score routing *style*, the failure the sub-score exists to reduce). **New `/decisions` item:** how `orphans` classifies E/F/G — broaden `convention-discovered`, add named classes, change candidate determination (out of TBD-14 scope), or accept as genuine; resolve-or-defer at the gate. TBD-14 stays Open; `orphans` stays out of TBD-10 weighting until a follow-up re-validation shows every residual classifying. No number set. **Update 2026-08-26 (`/decisions` CLASSIFICATION RULING — no code): `planning/decisions/2026-08-26_tbd-14-convention-runtime-class.md`.** E/F/G resolved into ONE unified accepted class **`convention/runtime-discovered (non-routed-by-design)`** (folds in the former `SKILL.md` convention-discovered class B), boundary = an **enumerated** convention set growing only by explicit ruling; conventions enumerated: (1) skill discovery, (2) agent-runtime config `.claude/**`/`WARP.md`/`cursor-hooks`, (3) component-manifest content (app-store `DESCRIPTION.md`), (4) test-harness fixtures (recognized by the harness that enumerates the dir, never a bare `test/` prefix). G investigated: 17 harness-globbed fixtures fold in; 1 test-dir doc (`MSW_USAGE_GUIDE.md`) stays `genuine-abandoned`. Rejected: separate classes per convention (reopens TBD-14 each new convention). MECHANISM (edges vs exclusion) out of scope — downstream design. Design §3.3/§3.4 amended; no code, no number (`TBD_10_WEIGHTS`/`ROUTING_LAYER_KEYS` untouched). **TBD-14 still Open** — close now requires a follow-up categorical re-validation applying these classes (its own session); a residual reached by a non-enumerated convention returns to `/decisions`, never a silent pass. **RESOLVED 2026-08-26 — re-validation run #2 SATISFIED the categorical gate (`planning/calibration/2026-08-26_context-audit-tbd-14-revalidation-run2.md`).** All **1 077** residual orphans classify (519 convention/runtime-discovered + 349 route-to-dir-nested + 189 dated-archival + 20 genuine-abandoned); **zero** reached by a non-enumerated convention; **zero** fix defects. Run #1's 162 unnamed residuals now resolve (E→runtime-config, F→component-manifest, G's 17 fixtures→test-harness; 1 doc `MSW_USAGE_GUIDE.md`→genuine-abandoned). (Also corrected run #1's arithmetic: residue total is **1 077 / −69**, not 1 087 / −59.) Fix verified correct at build; classification verified against real harness code. **`orphans` is now weighting-ELIGIBLE** (exclusion lifted); the weight NUMBER stays deferred (TBD-10, data-blocked). `coverage` is no longer the sole load-bearing headline sub-score — `orphans` and `routing_drift` are now eligible; only `bloat` remains shape-blocked pending TBD-11. No number set; `TBD_10_WEIGHTS`/`ROUTING_LAYER_KEYS` untouched.
| TBD-15 | `context_audit` v1.1 AGENTS.md-only follow-ups (deferred from the AGENTS.md router-recognition build, `planning/decisions/2026-08-20_agents-md-router-recognition.md`): (a) an `AGENTS.md`-only repo (no `CLAUDE.md` entry) reports `root.method: "claude_md"` — a distinct `agents_md` method value is the clean fix; (b) an empty/content-free `AGENTS.md`-only root surfaces only as `routing_unresolved` (info), not `root_empty` (critical), because the `root_empty` probe in `index.ts` matches the literal `claude.md` filename — the probe should key off the anchoring router doc regardless of name. Both share the same root cause (the accepted `root.method` label limitation) and should be resolved together. | v1.1 polish — nothing blocked; current behavior is non-silent (empty root shows info finding + null headline) | Open | |
| TBD-16 | `context_audit` **`routing_drift` precision** — a path-shaped backtick in router prose is resolved against **the router's own directory**, but router prose very often describes a *different* directory. What actually qualifies as a route, versus a path mentioned in prose? | **Blocks TBD-10** (`routing_drift` carries the joint-highest stub weight, `3`); `routing_drift` sub-score credibility; the README sample | Resolved (**2026-08-25 by re-validation run #2**) | Evidence: `planning/calibration/2026-08-24_context-audit-run-6-nine-repo-rerun.md` §3. All **59** drift findings across the four repos that produce any were checked against the filesystem: **26 prose-relative false positives** (target exists, one directory deeper — e.g. posthog's `products/signals/skills/AGENTS.md` says "the generalist keeps `references/conventions.md`", which resolves to a missing sibling path while `signals-scout-general/references/conventions.md` **exists**), **10 placeholder tokens** (`<chart_id>`, `<dir>`, `foo`), leaving **~10 plausibly genuine → precision ≈ 17 %**. Three named sub-classes: **prose-relative** (dominant), **install-target** (caveman routers describing where files land in the *consumer's* repo — `.windsurf/rules/`, `.clinerules/`, `.github/copilot-instructions.md`), and **bare-literal** (`.md` alone; posthog `products/stamphog/AGENTS.md:53` + caveman). **This ABSORBS the former standalone "caveman-28 drift residue"** — it was never caveman-specific; posthog reproduces it independently, satisfying the do-not-fix-on-one-repo rule. **Also note:** broken **markdown** links in routers produced their first-ever hits this run (posthog, 2) and **both were template placeholders** (`chart:<chart_id>`, `chart:<id>`) — md-link drift has still never fired on a genuine defect across nine repos and six runs. Same *shape* as TBD-13 (illustrative links inflating `broken_ref`) but scored, not demoted to info. Per task-observer obs #10, treat as a **definition** problem (tighten what counts as a route, and write it AS the definition), not a filter bolted on top. **`/decisions` 2026-08-24 — RESOLVED (policy + shape); fix authorised as its own build loop; TBD-16 stays Open until it lands.** See `planning/decisions/2026-08-24_routing-drift-precision-and-interim-disposition.md`. **(D1) Scoped as PRECISION + DISPLAY, not a weighting question** — at zero weight the ~49 wrong flags still render, so precision cannot be parked behind TBD-10. **(D2) `routing_drift` contributes `null` to the headline — a CORRECTNESS-driven null, not a data null.** Every other `null` here means "empty population/basis"; this one means **"the measurement is not trusted."** It is **NOT** resolved by assigning a weight later. **Restore trigger: the base-dir precision fix landing AND being re-validated against the nine-repo corpus.** Reuses the existing null-drop + weight-renormalisation + §5 guard untouched; **no weight-0 path invented** (weight 0 was measured and rejected — icm-architect 23 → **90**, hygiene-only `bloat` carrying the headline). **(D3) `routing_path_missing` demoted to `info` — INTERIM and REVERSIBLE**, precedent TBD-13's `broken_ref` move: still reported, unscored, visibly lower-confidence, so the ~10 genuine catches stay visible while the ~49 stop reading as high-severity. **`info` is NOT its permanent home — restore trigger: on the precision fix landing and being re-validated, `routing_path_missing` returns to `high`.** Suppressing the category entirely was **rejected** (would abandon the ~10 real broken routes and recreate the pre-2026-08-20 "structurally incapable of firing" state). D2 and D3 are **paired and revert together** on the same fix. **(D4)** caveman-28 **absorbed** — posthog reproduces every sub-class, so it was never caveman-specific. **(D5)** The base-dir over-read fix **and** the md-link-drift disposition (0 genuine findings in 9 repos / 6 runs) are **authorised as a separate build loop** — treat as a **definition** problem, not a filter; note that "add the described directory as a third base" is *not* obviously right (needs prose semantics; risks accidental matches + a new false-negative class). Output-contract-visible: `src/API.md` + `SEVERITY_BY_CATEGORY` update in the same commit as **that** code change (rule 8), **not** in the docs pass. **No number set.** **Ratchet ruling 2026-08-24 — D2/D3 were SUPERSEDED BEFORE IMPLEMENTATION; there is no interim state and no restore step.** See `planning/decisions/2026-08-24_d2-d3-superseded-before-implementation.md`. Gate 2 is docs-only, so neither measure was ever coded — on `main` at `9869d22`, `score.ts` still carries `routing_path_missing: "high"` and `index.ts` still scores `routing_drift` normally. The precision fix therefore lands **directly on current code**; the design's §3.4 exit criterion moves from **restore** to **confirm** (`routing_path_missing` confirmed never lowered and standing at `high`; `routing_drift` confirmed scored-real), asserted in the fix's own tests with **no lower-then-raise**. Materialise-then-revert was **rejected** — it manufactures history, shipping a state nobody runs so a record's tense stays true; this project corrects the record to match reality, never the reverse. D2/D3's substance is **not** re-litigated, and the paired-coherence requirement is kept in full. **TBD-16 still closes the same way** (fix + re-validation under the categorical close condition). Design doc: `planning/designs/2026-08-24_routing-drift-precision-design.md`. **`/decisions` 2026-08-24 (2nd pass) — tier-2 SCOPE reshaped after the plan review returned REJECT.** See `planning/decisions/2026-08-24_tier-2-scope-and-placeholder-globality.md`. **(D1)** An all-unanchored router yields `routing_drift: null` and that is **correct** — no population to compute a rate over; an ordinary **data null** (`subscoreFromCount`'s `n === 0` contract, same logic as the D3 coverage guard), not D2's correctness-null. §3.4's "scored-real" was **over-stated, not the behaviour**: now "scored-real whenever a scoreable population exists; `null` only when a router's entire reference set is unanchored." **(D2)** **Root-located routers get NO tier 2** — strict anchored-or-drift. Their "own subtree" is the whole repo, so the bound is vacuous: observed, the identical prose span in a root vs. a nested router got **opposite verdicts**. **`"root-located"` means `relPath` contains no `/` — NOT `isRoot`**, which in this codebase means "is a router doc" at any depth and already gates the backtick branch; implementing it as `isRoot` would make tier 2 never fire and silently delete the fix. Measured on the **pinned corpus**: tier 2 still fixes **17 of 26** prose-relative FPs (all posthog, the canonical case), gives back **9** (all caveman's root `CLAUDE.md`). The reviewer's narrower multi-segment-tail alternative was **considered and not taken**. **(D3)** Placeholder exclusion **RATIFIED as global**, not router-scoped — a form-with-a-blank is not a path in any syntax *or any doc type*; it also stops non-router `broken_ref` (observed 2 → 1). Ratification, not reversal. **(D4)** The two out-of-corpus repos used as evidence are **correctness probes, quarantined from all threshold reasoning**; re-validation is against the **pinned nine-repo corpus only**. §3.5 gains "prose-relative under a root-located router" as a **named accepted false positive** — load-bearing, since §3.4's categorical close condition would otherwise bounce those 9 as unclassifiable (precedent: `2026-08-20_router-path-drift.md` §33). **The plan is REJECT and must be revised against the amended design and re-reviewed before any code.** **`/decisions` 2026-08-25 (3rd pass, 4th ratchet trip) — `<token>` PRECEDENCE.** See `planning/decisions/2026-08-25_placeholder-vs-commonmark-destination-precedence.md`. The CommonMark `<dest>` strip (added by a plan re-review to stop `[x](<docs/gone.md>)` vanishing) collided with §3.2, which names `<dir>` as template text: stripping left a bare path so `<dir>` drifted while `{name}` was excluded — and the `src/API.md` text the plan told the implementer to write named `<dir>` as an EXCLUDED example, so the public contract would have asserted behaviour the code lacked. **Ruling: placeholder detection is adjudicated FIRST.** A `<…>`-wrapped **token** is a placeholder (excluded); a `<…>`-wrapped **path** is a delimiter (stripped, and a broken one still drifts, preserving test T2d). **Discriminator:** the wrapper is a delimiter when its content contains `/` **or** ends in a file extension — slash-only was rejected because it swallows `<my file.md>` (a spaced destination is the canonical reason CommonMark has angle brackets), extension-only because it swallows `<docs/gone>`. The literal reading (test the raw string, skip the strip) is **explicitly rejected** — it breaks T2d. A code-shaped choice, recorded here rather than settled inside an execution plan, per the ratchet. **The close condition is unthreatened:** run-6's only md-link placeholder hits were `chart:<chart_id>`/`chart:<id>`, partially wrapped and excluded correctly either way, so no measured number and no §3.5 class moves. **The `src/API.md` text does not ship until this lands, and the five mechanical plan fixes are sequenced after it** (finding 1's correction depends on this ruling). No number set. | **Re-validation 2026-08-25 (`planning/calibration/2026-08-25_context-audit-tbd-16-revalidation.md`): CLOSE NOT SATISFIED — TBD-16 stays Open.** The fix landed (PR #26, `main` `4d40be6`, 98/98) and reproduced the predicted headline (drift 59→28; 17/26 prose-relative fixed all-posthog; 9 given back all-caveman-root; 11 nested residuals verified not tier-2 misses). But §3.4's CATEGORICAL gate failed: of the 28 residuals, 18 classify into named §3.5 classes and 3 are plausibly-genuine broken routes, leaving **7 false positives from THREE mechanisms §3.5 does not name** — (A) lexical placeholders (`foo`, `...` ellipsis), (B) prose reference/citation (`CONTEXT.md`/`SKILL.md`/`bench/agent/README.md` used referentially, not as routes), (C) prose-relative under a NESTED router with the target OUTSIDE its own subtree (posthog canvas → an ancestor `products/desktop/docs/`; icm `setup/questionnaire.md` → a depth mismatch). **D2/D3 do NOT flip** (routing_drift not confirmed scored-real; routing_path_missing not confirmed high). **Sixth `/decisions` trip pending** on naming-or-fixing A/B/C. No threshold; the 21/28 accounted ratio is not a pass (rule 7). **Sixth `/decisions` trip RESOLVED 2026-08-25 (`planning/decisions/2026-08-25_tbd-16-residual-fp-mechanisms-abc.md`; §3.5 amended).** Six dispositions on the three mechanisms, each split on measured merits: **(A)** `...`-ellipsis → SHAPE EXCLUSION (authorized-pending; no real corpus path contains `...`), compound placeholder `signals-scout-foo` → ACCEPTED §3.5 class (indistinguishable from the real `signals-scout-*` family, prose-only marker); **(B)** bare-filename-no-path (`CONTEXT.md`, `SKILL.md`) → SHAPE EXCLUSION (authorized-pending; no resolving bare-filename route in corpus), path-shaped citation `bench/agent/README.md` → ACCEPTED §3.5 class WITH NAMED LIMITATION (tool can misread a prose citation as a route; doesn't read surrounding prose); **(C)** splits into opposite legal dispositions — **C2** ancestor-located → ACCEPTED (fix ≈ repo-wide, the bound §3.1 rejected), **C1** in-subtree exact-tail-miss → ACCEPTED (only basename-only reaches it: yield 1 non-recurring, masks 4 unrelated findings incl. a 34-way SKILL.md collision — the silent-swallow direction refused). **Four accepted classes are in §3.5 now (no code); two shape exclusions are forward-referenced only (still flagged today).** **CLOSE PATH: TBD-16 stays OPEN — closes only after (a) a build loop implementing the two shape exclusions (design→writing-plans→TDD→review→finishing-a-development-branch, with its own src/API.md edit per rule 8), then (b) re-validation run #2 against the pinned corpus under §3.4's categorical gate. D2/D3 stay unconfirmed until run #2 passes.** No API.md change this pass (no code). No threshold. **CLOSED 2026-08-25 — re-validation run #2 (`planning/calibration/2026-08-25_context-audit-tbd-16-revalidation-run2.md`) SATISFIED §3.4's categorical gate.** The two shape exclusions landed (PR #30, `main` `94977ec`, 102/102); drift residue 28→16, all 16 classify (15 named §3.5 classes + 1 verified genuine broken route: caveman `EFFICIENT_AGENT_BUILDER_SPEC.md`, an absent "Authority:" doc). The exclusions removed 12 findings, every one verified prose/placeholder — zero FN. **D2 and D3 CONFIRMED together:** `routing_drift` is scored-real (correctness-null lifted), `routing_path_missing` stays `high`. Two run-#1 corrections folded into the run-#2 record: (A) `report.md` was mislabelled plausibly-genuine, is prose (run-#1 outcome unchanged); (B) the prose-relative-under-root class corrected 9→3 (7 were bare filenames, now shape-excluded). No threshold/weight number; `TBD_10_WEIGHTS`/`ROUTING_LAYER_KEYS` untouched.
| TBD-17 | `context_audit` **further router conventions** — three routing conventions the parser does not read were found by run-6's pre-flight. Should any be added in v1? | Router-name set / link parser; recorded so a future session does not re-discover and guess | **Resolved** | Evidence: run-6 §6 — **all three were measured, not assumed, and all three are low-yield** (unlike `AGENTS.md`, none is a v1 STOP). **(a) `@`-import lines** (`@./path`) — the Claude Code / Gemini CLI context-import syntax, arguably the strongest possible routing edge, but across all nine repos recognized routers hold only **7** genuine `@`-import edges (posthog 3, caveman 4) against caveman's 385 and posthog's 431 resolved refs. Parsing is also **hazardous**: a naive `^@` rule eats Python decorators — superset's `mcp_service/CLAUDE.md` alone has 11 `@tool(`/`@pytest.fixture` lines in code fences, posthog has `@action(...)` — so any rule must exclude fenced blocks and require a path shape. **(b) `GEMINI.md`** — present 3/9, but superset's is a **symlink → `AGENTS.md`** (D2 correctly deduped it, and a `GPT.md → AGENTS.md` symlink alongside it); the two real files are 92 B and 131 B holding only `@`-imports to skills already routed elsewhere → ≈ zero added signal. **(c) `Ghost/CONTEXT-MAP.md`** — a genuine hub router, but the three `CONTEXT.md` files it lists are **already found directly**, so it would add 3 edges to Ghost's 34; 1/9 repos. **RESOLVED `/decisions` 2026-08-24 — add no new router filename and no new link syntax in v1.** See `planning/decisions/2026-08-24_tbd-17-no-new-router-syntax-v1.md`. `AGENTS.md`/`CLAUDE.md`/`CONTEXT.md` remain the complete router-name set; markdown links + backtick code-span paths remain the complete edge syntax. This is **rule 7 / D7 applied to measured evidence, not to a guess** — D7 set the condition ("until a run shows the population using it"); run-6 is that run and the answer is **no**: the population does carry these conventions, at a yield that does not justify the parse risk. The bar is the one `AGENTS.md` cleared (root layer invisible on 4/4, every sub-score an artifact); none of the three is close. **`@`-imports additionally require fenced-code awareness the parser does not have** (`2026-08-20_backtick-routing-edges-and-orphans-guard.md` §73 records that fenced blocks are not parsed specially). **Reopens only if** a future run shows a convention carrying a **material share** of routing edges **and** parseable without a new false-positive class. **No code change; nothing to update in `src/API.md`.** Also recorded: superset ships **three** root aliases to `AGENTS.md` (`CLAUDE.md`, `GEMINI.md`, `GPT.md`) and D2 correctly emitted no `symlink` finding for any; superpowers ships `AGENTS.md → CLAUDE.md`, the reverse direction, also deduped correctly. |
| TBD-18 | `context_audit` **`orphans` sub-score basis** — the sub-score is `1 − orphaned/candidates` over ALL orphan candidates, but TBD-14 (closed 2026-08-26) established that the overwhelming majority of orphans are ACCEPTED layout classes, not rot. Should the sub-score net out the TBD-14 accepted classes so it measures genuine-abandoned rot, or is candidate-counting correct and the accepted-layout pull handled at weighting? | `orphans` sub-score; **blocks `orphans` carrying weight in TBD-10** | **Resolved 2026-08-26** (status-cell corrected 2026-08-27; the detail below already recorded the close) | **Surfaced 2026-08-26 by run-7** (`planning/calibration/2026-08-26_context-audit-run-7-numbers-calibration.md` §4.2), the first run to weight all four shape-clean sub-scores together. Of TBD-14's **1 077** residual orphans across the pinned nine-repo corpus, only **20 are `genuine-abandoned`**; the other 1 057 are accepted layout (convention/runtime-discovered 519, route-to-directory-nested 349, dated-archival 189). The `orphans` sub-score counts all candidates, so low scores (superset **1**/n113, claude-mem **0**/n61, posthog 11/n678) measure repo **layout style**, not routing failure — the exact layout-vs-rot conflation TBD-14 spent four runs proving the *findings* must not make, now reappearing in the *sub-score→weight* mapping. **This is NOT a reopening of TBD-14** (whose findings + classification are correct and closed); it is the distinct sub-score-definition question that only becomes visible when `orphans` is assigned a weight. Candidate directions (none chosen): (a) re-base the sub-score on genuine-abandoned only (net out the four accepted classes — changes what `orphans` scores, its own build loop); (b) keep candidate-counting and bound the damage with a low `orphans` weight in TBD-10; (c) a hybrid (score genuine-abandoned as the numerator, keep the full candidate set enumerated in findings for transparency). **Stub-and-continue:** `orphans` sub-score is unchanged (still `subscoreFromCount(g.orphanCount, g.orphanCandidateTotal)` in `index.ts`); no code touched; `orphans` stays out of TBD-10 weighting until resolved. Resolve at `/decisions`; if it needs a design, `superpowers:brainstorming` first. **No number set.** **DIRECTION RESOLVED 2026-08-26 (`/decisions`, owner-ratified, RECORD-ONLY — `planning/decisions/2026-08-26_tbd-10-weights-partial-and-tbd-18-orphans-rebase.md`): re-base the `orphans` sub-score onto genuine-abandoned rot** (net out the TBD-14 accepted-layout classes). Bound-with-low-weight rejected (leaves the sub-score measuring style). **No code this session — `orphans` scoring untouched; it is simply EXCLUDED from the headline until this builds** (stronger than a low weight — cannot mislead). Build is a separate loop: `superpowers:brainstorming` → design → TDD → re-validate; the TBD-10 `orphans` weight stays gated behind it. **TBD-18 stays Open (build pending).** **Update 2026-08-26 — DESIGN landed (`superpowers:brainstorming`, PR #47): `planning/designs/2026-08-26_orphans-genuine-abandoned-rebase-design.md`.** Shape = **numerator-only** (`orphans = 1 − genuineAbandonedCount / orphanCandidateTotal`; denominator unchanged; all-accepted repo → ~100). Scored/not-scored split is distinct from `FURNITURE` (accepted-layout orphans stay candidates + findings). **Four tight detectors** net out ~741+ of the ~1 057 accepted docs: **D1** route-to-dir-nested (structural, runs on the post-TBD-14 residual set), **D2** skill-discovery (ancestor has `SKILL.md`), **D3** agent-runtime config (`.claude/**`, `WARP.md`, `cursor-hooks/**`), **D4** tight dated-archival (D4a dated-filename structurally tight; D4b `plans/`/`CHANGELOG/` convention-tight). **Named visible-FP gaps → `/decisions`** (not netted in v1, ~127 docs stay counted): component-manifest (needs registry knowledge), test-harness fixtures (ruling forbade path-prefix — MSW proof), bare `docs/**` (loose). **Close condition:** categorical re-validation on the pinned corpus with D4b's `plans/`/`CHANGELOG/` nets checked **individually** (the one silent-FN vector). `orphans` weight stays gated under TBD-10. **Next: user reviews the spec → `superpowers:writing-plans` → TDD build (its own session).** **BUILT 2026-08-26 (subagent-driven-development, PR pending, branch `feat/tbd-18-orphans-rebase`):** the numerator-only re-base is implemented and landed — `src/tools/context-audit/accepted-layout.ts` (D1–D4 + `isAcceptedLayout`), `graph.ts` `genuineAbandonedCount`, `index.ts`/`types.ts` sub-score + `stats.genuine_abandoned_count`, API.md/ledger (252 unchanged), 126/126 tests, `tsc` clean, whole-branch review clean. **But the §6 categorical re-validation FAILS the close condition** (`planning/calibration/2026-08-26_context-audit-tbd-18-revalidation.md`): fidelity + reconstruction pass on all nine repos and the orphan population is unchanged at 1 077, BUT **two confirmed silent-FN vectors** surface — (1) **§6.1 D1 basis over-broad:** D1 keys on the broad `routedDirs` (which includes *file-parent* additions and root `""`), not directory-**target** routes; Ghost `apps/admin/test-utils/posts-analytics/MSW_USAGE_GUIDE.md` (one of the 20 genuine-abandoned, netted because `apps/admin` is in `routedDirs` via a link to `apps/admin/README.md`) → 19/20 survive, one silent FN; (2) **§6.2 D4b `plans/` loose:** 2 live ready-to-build posthog PRDs under `products/desktop/docs/plans/` are netted (32/34 D4b-sole nets are legit superset `CHANGELOG/` version archives; 2 are live docs). **TBD-18 therefore does NOT close.** Both defects route to `/decisions` as **TBD-19** (D1 basis) and **TBD-20** (D4b disposition); `orphans` weight stays gated behind them. **TBD-18 stays Open** (closes only after TBD-19 + TBD-20 resolve and re-validation re-passes). **RESOLVED 2026-08-26 — TBD-19 + TBD-20 resolved and the categorical re-validation SATISFIED the §6 close condition (`planning/calibration/2026-08-26_context-audit-tbd-19-20-revalidation.md`).** The `orphans` sub-score is now the numerator-only genuine-abandoned re-base with both detector-basis defects fixed: fidelity 9/9, reconstruction 9/9, all 20 canonical genuine survive (MSW counted), the 32 D4b version-shape nets are all genuine version archives, and the 2 live posthog PRDs are counted; orphan population unchanged at 1 077. **`orphans` is now weighting-ELIGIBLE** (exclusion lifted); the weight NUMBER stays deferred under TBD-10 (data-blocked). `TBD_10_WEIGHTS`/`ROUTING_LAYER_KEYS` untouched. |
| TBD-19 | `context_audit` **D1 (route-to-directory-nested) basis** — `isRouteToDirNested` keys on the broad `routedDirs`, but `graph.ts` populates `routedDirs` from *file-parent* additions (a router linking `dir/file.md` adds `dir`) and root `""` as well as genuine directory-target routes. Design §D1 intended "nested below a routed **directory-target**." The broad basis silently nets genuine rot. | `orphans` sub-score numerator (D1); **blocks `orphans` carrying weight in TBD-10** (with TBD-20) | **Resolved** | **Surfaced 2026-08-26 by the TBD-18 §6 re-validation** (`planning/calibration/2026-08-26_context-audit-tbd-18-revalidation.md` §2) and pre-flagged by the whole-branch reviewer (Rec #2). **Confirmed silent FN:** Ghost `apps/admin/test-utils/posts-analytics/MSW_USAGE_GUIDE.md` — a canonical genuine-abandoned doc (re-val #2 §3.2, the MSW proof of the test-fixture caution) — is D1-sole-netted because its ancestor `apps/admin` sits in `routedDirs` **only** because root `CLAUDE.md`/`AGENTS.md` link the file `apps/admin/README.md`. 378 nets are D1-sole; 0 are root-`""`-only, so the driver is file-parent routed-ancestor dirs, not root. **Candidate direction (none chosen, rule 7 — no guess):** re-base D1's ancestor test onto the **directory-target** set (`dirTargetsBySrc`, which `graph.ts` already computes for reachability but does not expose), instead of `routedDirs`. This is a change to D1's input set → a design decision. **Stub-and-continue:** D1 unchanged in code; `orphans` stays out of TBD-10 weighting. Resolve at `/decisions`; if it needs a design, `superpowers:brainstorming` first. **No number set.** **DIRECTION RESOLVED 2026-08-26 (`/decisions` — `planning/decisions/2026-08-26_tbd-19-tbd-20-d1-basis-d4b-disposition.md`): re-base both of `isRouteToDirNested`'s checks onto the flattened union of `dirTargetsBySrc` values (the directory-**target** set), all-source not reached-restricted (D1 evaluates only the residual set; the union is a structural "does this repo route to this directory" fact). Expose `dirTargets: Set<string>` from `GraphResult`, thread into `AcceptedLayoutCtx`; `routedDirs` stays for other consumers. `routedDirs` was polluted by file-parent additions + root `""`, which is what silently netted MSW; the narrower set restores the structural property §D1 already claimed. TBD-18 design §D1/§5 amended same as TBD-20 (one design edit). Build is one loop with TBD-20 under TDD; closes on the categorical re-validation re-passing (MSW among the counted, 20 genuine survive). No number set; `TBD_10_WEIGHTS`/`ROUTING_LAYER_KEYS` untouched.** **RESOLVED 2026-08-26 — BUILT + re-validation SATISFIED (`planning/calibration/2026-08-26_context-audit-tbd-19-20-revalidation.md`).** At build the straight `dirTargets` swap re-validated to a **second silent FN** (a doc whose parent is a file-parent-routed dir matched a DISTANT `dirTargets` ancestor through the intervening file-parents — the live posthog PRDs via `products` 3 levels up, + 84 `docs/internal/**`). The as-built D1 uses the **nearest-routing-known-ancestor rule** with BOTH sets: `isRouteToDirNested(rel, routedDirs, dirTargets)` nets iff the first ancestor in `routedDirs` is a strict-ancestor dir-target. `routedDirs` locates the nearest routing-known ancestor (the shield); `dirTargets` decides whether it is a real directory route. Root `""` filtered out of `dirTargets` (reviewer Minor #1). Re-validation: fidelity 9/9, reconstruction 9/9, **20/20 genuine survive (MSW counted)**; orphan population unchanged at 1 077. Tests 130/130; `tsc` clean; ledger 252 (rule 2); no `src/API.md`/schema change from D1 (internal). `TBD_10_WEIGHTS`/`ROUTING_LAYER_KEYS` untouched. |
| TBD-20 | `context_audit` **D4b (`plans/`/`CHANGELOG/` segment) disposition** — D4b is convention-tight, not structural: it nets any doc under a `plans` or `CHANGELOG` directory segment on the *guess* that such segments are archival. The pinned corpus breaks the guess. | `orphans` sub-score numerator (D4b); **blocks `orphans` carrying weight in TBD-10** (with TBD-19) | **Resolved** | **Surfaced 2026-08-26 by the TBD-18 §6.2 individual D4b check** (`planning/calibration/2026-08-26_context-audit-tbd-18-revalidation.md` §3) — exactly the silent-FN vector design §5/§6.2 named. Of 34 D4b-sole nets, **32 are legitimate** (superset `CHANGELOG/<version>.md` release archives) but **2 are live, ready-to-build PRDs** (posthog `products/desktop/docs/plans/browser-tabs.md` `Status: ready-for-agent`; `.../plans/skills-tab.md` `Status: Ready to build`, last-updated 2026-06-11), indexed by a sibling README — netting them silently removes live docs from the rot score. **Candidate directions (none chosen):** drop D4b entirely (keep only D4a dated-filename, structural); tighten D4b (require a dated filename or a version-shaped sibling set under `CHANGELOG/`); or keep with a documented per-repo carve-out. **Stub-and-continue:** D4b unchanged in code; `orphans` stays out of TBD-10 weighting. Resolve at `/decisions`. **No number set.** (Design §9 item 4 pre-registered this exact deferral.) **DIRECTION RESOLVED 2026-08-26 (`/decisions` — `planning/decisions/2026-08-26_tbd-19-tbd-20-d1-basis-d4b-disposition.md`): drop BOTH the `plans/` and `CHANGELOG/` directory-segment rules; replace with a STRUCTURAL version-shaped-basename net folded into D4a.** `plans/` is a confirmed silent-FN vector (2 live posthog PRDs); dated plans stay covered by D4a. `CHANGELOG/`'s 32 legit superset `CHANGELOG/<semver>.md` archives are recovered structurally by netting a full-semver basename `^v?\d+\.\d+(\.\d+)+$` (≥2 dots; `1.4.1`/`6.1.0` net, ambiguous `v2`/`2.0` do not → counted, safe). Same class as D4a's dated-filename self-evidence — no directory-name convention guess. On-corpus effect: exactly the 2 live PRDs flip to counted; all 32 archives stay netted. Rejected: drop-D4b-with-no-replacement (loses 32 archives to visible FP needlessly); per-repo carve-out (does not generalize). Exact regex pinned in TDD with boundary tests; every version-shape net checked individually in the re-validation (successor to §6.2's D4b check). Build is one loop with TBD-19. No number set. **RESOLVED 2026-08-26 — BUILT + re-validation SATISFIED (`planning/calibration/2026-08-26_context-audit-tbd-19-20-revalidation.md`).** `isTightDatedArchival` now nets D4a (dated filename) OR D4b (version-shaped basename `^v?\d+\.\d+(\.\d+)+$` on the `.md`-stripped basename); the `plans/`/`CHANGELOG/` directory-segment rules are gone. Re-validation: the **32** D4b version-shape-sole nets are all superset `CHANGELOG/<semver>.md` release archives (each a genuine version archive, none live); the **2 live posthog PRDs** under `products/desktop/docs/plans/` flip to **counted** (protected by both the D4b change and the refined D1). `src/API.md` class-name phrase → `tight dated/versioned-archival` (rule 8, same commit); no schema change. Tests 130/130. |
| TBD-21 | `override_log` **`date` format validation** — v1 validates `date` for **presence only**; a malformed / non-ISO `date` string is rendered as-is, not caught. Should `date` be validated as a real ISO-8601 date and rendered canonically? | `override_log` v1.1 date-format polish — **nothing blocked in v1** (presence-only ships; a *missing* `date` is already a high-severity `override_field_missing` finding, a *malformed* one renders verbatim). | Open | Opened 2026-08-27 by Gate 2 (`/decisions`) for the `override_log` build; see `planning/decisions/2026-08-27_override-log-v1-scope-and-validation.md` (Open call 2). **Stub-and-continue:** the validator checks presence (non-empty after trim) only; the ISO-format check + canonical render is the deferred v1.1 item. `date` is a rendered anchor — nothing computes on it — so a loose value degrades display, not scoring. Resolve at a later `/decisions`; no guess (rule 7). |
| TBD-22 | `doc_drift` **canonical self-discovery (read-files mode)** — v1 takes a caller-supplied canonical schema; a mode where the tool reads a doc + a committed canonical artifact and extracts/aligns the pair itself is deferred. **Rejected for v1 by Findings 1–2** (`planning/decisions/2026-08-27_doc-drift-v1-scope-and-diff-sides.md`): B-A-MCP emits no committed canonical artifact (schemas are TS literals in each tool's `index.ts`), and the doc deliberately diverges from the minimized emitted schema, so a self-discovered canonical would diff against the wrong representation. | `doc_drift` v1.1 self-service mode — **nothing blocked in v1** (provided/inline ships) | Open | |
| TBD-23 | `doc_drift` **orientation walk-test** — the founding design's check (b): can a memoryless agent still orient in the workspace after drift (a continuous walk test, not a scaffold-time check). Deferred — **redundant with `context_audit`'s orphans/coverage/routing_drift** ("can an agent orient" is already what those measure) until proven distinct. | `doc_drift` v1.1/Phase 2 — **nothing blocked in v1** | Open | |
| TBD-24 | `doc_drift` **framework migration/route parsing** — the original open-ended TBD-9 framing (parse migrations and routes across arbitrary frameworks). Deferred to Phase 2 — the unbounded scope the v1 narrowing exists to avoid. | `doc_drift` Phase 2 — **nothing blocked in v1** | Open | |
| TBD-25 | `context_audit` **`orphans` test-harness-fixture mechanization** (§4-gap, TBD-14 convention #4) — net markdown test-data files a harness discovers by enumerating a fixture dir. Deferred from the component-manifest loop (`planning/decisions/2026-08-28_component-manifest-detector-mechanism.md` L12): the tool reads no source, so it cannot see the harness glob; the only source-free rule (harness-config-in-an-ancestor) is a path-prefix in disguise that silently swallows `MSW_USAGE_GUIDE.md` (the Ruling-2 casualty). Per the tie-breaker, no rule beats a loose rule — the class stays **counted** (visible FP) until a tight source-free mechanism appears. | `orphans` §4-gap; one of the three items gating the TBD-10 `orphans:1`→final raise | Open | |
| TBD-26 | `context_audit` **`orphans` bare-`docs/**` disposition** (§4-gap) — the third §4-gap item: how `orphans` treats a loose top-level `docs/**` tree (routed-by-convention vs genuinely orphaned). Not addressed in the component-manifest loop (`planning/decisions/2026-08-28_component-manifest-detector-mechanism.md` L13). | `orphans` §4-gap; one of the three items gating the TBD-10 `orphans:1`→final raise | Open | |

> **TBD policy:** If a TBD blocks implementation, stub it, leave a `TODO: TBD-XXX` comment, and continue. Do not guess. When a TBD is resolved, record the reasoning in `planning/decisions/YYYY-MM-DD_title.md`.
